---
phase: 06-writeup-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DOC-02-final-report.tex
  - docs/sections/01-intro.tex
  - docs/sections/bibliography.tex
  - Makefile
  - .gitignore
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "docs/DOC-02-final-report.tex compiles via `make paper` on the target laptop (latexmk exit 0)"
    - "Main .tex uses \\input{sections/...} for modular bodies so Plans 04 and 05 can write section files in parallel without touching the main file"
    - "Page 1 opens with compare_mrc_2panel.pdf + the D-04 surprise-finding callout"
    - "Bibliography includes all 6 canonical citations (Zhang/Yang/Waldspurger/Einziger/Caffeine/Clauset)"
  artifacts:
    - path: "docs/DOC-02-final-report.tex"
      provides: "LaTeX main file — preamble + title + page-1 figure + \\input{} stubs for every section + bibliography include"
      min_lines: 60
    - path: "docs/sections/01-intro.tex"
      provides: "Introduction section — opens with the surprise finding hook (D-01), states the question the paper answers"
      min_lines: 15
    - path: "docs/sections/bibliography.tex"
      provides: "Two-column thebibliography with 6 canonical entries per 06-PATTERNS.md"
      min_lines: 12
    - path: "Makefile"
      provides: ".PHONY paper target that runs `cd docs && latexmk -pdf DOC-02-final-report.tex`"
      contains: "paper:"
  key_links:
    - from: "docs/DOC-02-final-report.tex"
      to: "docs/sections/*.tex"
      via: "\\input{sections/NN-name}"
      pattern: "\\\\input\\{sections/"
    - from: "docs/DOC-02-final-report.tex"
      to: "results/compare/figures/compare_mrc_2panel.pdf"
      via: "\\includegraphics on page 1"
      pattern: "compare_mrc_2panel"
    - from: "Makefile paper target"
      to: "latexmk -pdf DOC-02-final-report.tex"
      via: "shell recipe"
      pattern: "latexmk.*DOC-02"
---

<objective>
Create the LaTeX paper scaffold — preamble, title, page-1 figure hook, section
\input{} stubs, bibliography, and a `make paper` target — so Plans 04 and 05
can write body sections in parallel without contending on a single monolithic
.tex file.

Purpose: This is the **interface-first task** of the paper pipeline. All
downstream paper work depends on the main file's \input{} skeleton existing
and the build recipe working. Pattern established: modular .tex files in
docs/sections/ included by docs/DOC-02-final-report.tex; build via `make paper`.

Output: A compilable skeleton (empty/placeholder sections OK) that produces a
valid PDF when `make paper` runs. Plans 04 + 05 will fill the sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-writeup-demo/06-CONTEXT.md
@.planning/phases/06-writeup-demo/06-PATTERNS.md

<!-- Preamble / title / bibliography analogs -->
@progress.tex
@v7.tex

<!-- Build-recipe analog: .gitignore already covers *.aux, *.fdb_latexmk, *.fls, *.synctex.gz -->
@.gitignore

<interfaces>
<!-- Canonical figure path (all figures are already rendered; this plan cites them by relative path) -->
Page-1 figure:       ../results/compare/figures/compare_mrc_2panel.pdf
Conclusion figure:   ../results/compare/figures/winner_per_regime_bar.pdf
SHARDS figure:       ../results/shards_large/figures/shards_mrc_overlay.pdf

<!-- Bibliography keys (used by Plans 04 + 05 via \cite{...}) -->
\cite{zhang2024sieve}     — SIEVE NSDI'24
\cite{yang2023fifo}       — S3-FIFO SOSP'23
\cite{waldspurger2015shards} — SHARDS FAST'15
\cite{einziger2017tinylfu}   — TinyLFU ACM ToS '17
\cite{caffeine2024}          — Caffeine v3.1.8
\cite{clauset2009powerlaw}   — Power-Law Distributions SIAM Rev '09

<!-- Section module contract — Plans 04 and 05 will create these files -->
docs/sections/01-intro.tex         — THIS PLAN (opening hook from D-01)
docs/sections/02-related.tex       — Plan 04 (brief related-work)
docs/sections/03-workload.tex      — Plan 04 (workload characterization + table)
docs/sections/04-policies.tex      — Plan 04 (policy comparison + MRC figs)
docs/sections/05-mechanism.tex     — Plan 05 (WHY SIEVE≈WTLFU on Congress — deep dive)
docs/sections/06-shards.tex        — Plan 04 (SHARDS rigor + 4-rate table + fig)
docs/sections/07-ablations.tex     — Plan 04 (3 ablation figures)
docs/sections/08-byte-mrc.tex      — Plan 05 (byte-MRC variance warning)
docs/sections/09-conclusion.tex    — Plan 05 (decision tree + limits/v2)
docs/sections/bibliography.tex     — THIS PLAN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/DOC-02-final-report.tex with preamble, title, page-1 figure hook, and all \input{} stubs</name>
  <files>docs/DOC-02-final-report.tex, docs/sections/.gitkeep</files>
  <read_first>
    - progress.tex (verbatim source for preamble lines 1-17 and title-block lines 22-27 — copy the shape exactly)
    - v7.tex (source for hyperref + multicol usage; for two-column bibliography at lines 74-92)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md §"`docs/DOC-02-final-report.tex` — paper source" (lines 31-128; gives exact preamble snippet, title block, figure-inclusion pattern, tabular pattern, bibliography pattern, build command)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md §"decisions" D-01, D-02, D-04, D-11 (paper narrative + figure-led opening + figure count)
    - .gitignore (confirm *.aux, *.fdb_latexmk, *.fls, *.synctex.gz, docs/*.pdf handling — lines 50-55 already cover LaTeX artifacts; add docs/*.pdf entry if committing the PDF is deferred)
  </read_first>
  <action>
    Create directory docs/sections/ with a .gitkeep file (so the stubs directory is committed even before Plan 04/05 populates it). Create docs/DOC-02-final-report.tex with the following content verbatim (this is the skeleton — Plans 04 and 05 fill in the \input{} target files):

    ```latex
    \documentclass[11pt,letterpaper]{article}
    \usepackage[margin=0.8in,top=0.6in,bottom=0.6in]{geometry}
    \usepackage{booktabs}
    \usepackage{tabularx}
    \usepackage{enumitem}
    \usepackage{amsmath}
    \usepackage{microtype}
    \usepackage{fancyhdr}
    \usepackage{graphicx}
    \usepackage{hyperref}
    \usepackage{multicol}

    \pagestyle{fancy}
    \fancyhf{}
    \lhead{\small CS 2640: Modern Storage Systems $\cdot$ Spring 2026}
    \rhead{\small Final Report}
    \cfoot{\thepage}

    \setlength{\parskip}{3pt plus 1pt minus 1pt}
    \setlength{\parindent}{0pt}

    \begin{document}
    \thispagestyle{fancy}

    \begin{center}
    {\large\bfseries Caching Policy Evaluation for Legislative Data APIs:\\[-2pt]
    Why W-TinyLFU Wins on Court but Ties SIEVE on Congress}\\[3pt]
    {\small Mira Yu \hspace{1.5em} \today}
    \end{center}
    \vspace{-4pt}

    %% === Page 1 figure-led opening per D-04 ===
    \begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{../results/compare/figures/compare_mrc_2panel.pdf}
    \caption{Miss-ratio curves for six policies on Congress (left) and Court
    (right). Shaded bands are $\pm 1\sigma$ across 5 seeds.
    \textbf{Note the 4--5pp gap between W-TinyLFU and SIEVE on Court vs.\ the
    photo-finish on Congress at high $\alpha$ --- this paper explains why.}}
    \label{fig:opening}
    \end{figure}

    %% === Body sections (Plans 04 and 05 fill these) ===
    \input{sections/01-intro}
    \input{sections/02-related}
    \input{sections/03-workload}
    \input{sections/04-policies}
    \input{sections/05-mechanism}
    \input{sections/06-shards}
    \input{sections/07-ablations}
    \input{sections/08-byte-mrc}
    \input{sections/09-conclusion}

    %% === Bibliography ===
    \input{sections/bibliography}

    \end{document}
    ```

    Then create docs/sections/.gitkeep as empty file so git tracks the directory. The \input{} of section files that don't yet exist will cause latexmk to fail — that's expected until Plans 04 + 05 land.
  </action>
  <verify>
    <automated>test -f docs/DOC-02-final-report.tex && test -f docs/sections/.gitkeep && grep -c "\\\\input{sections/" docs/DOC-02-final-report.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/DOC-02-final-report.tex` exit 0
    - `test -f docs/sections/.gitkeep` exit 0
    - `grep -c '\\input{sections/' docs/DOC-02-final-report.tex` returns 10 (9 body + 1 bibliography)
    - `grep -q 'compare_mrc_2panel.pdf' docs/DOC-02-final-report.tex` exit 0
    - `grep -q '\\documentclass\[11pt,letterpaper\]' docs/DOC-02-final-report.tex` exit 0 (preamble matches progress.tex)
    - `grep -q '\\rhead{\\small Final Report}' docs/DOC-02-final-report.tex` exit 0 (per PATTERNS.md — changed from "Midpoint Report")
    - `grep -q '\\usepackage{graphicx}' docs/DOC-02-final-report.tex` exit 0 (required for \\includegraphics)
    - `grep -q '\\usepackage{hyperref}' docs/DOC-02-final-report.tex` exit 0
  </acceptance_criteria>
  <done>Main .tex file exists with all 10 \\input{} stubs and the page-1 figure hook; docs/sections/ directory is committable; the file ALONE will not compile yet (sections missing) — this is expected.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/sections/01-intro.tex (opening hook from D-01) and docs/sections/bibliography.tex (6 canonical citations)</name>
  <files>docs/sections/01-intro.tex, docs/sections/bibliography.tex</files>
  <read_first>
    - .planning/phases/06-writeup-demo/06-CONTEXT.md §"specifics" (lines 157-161 give the D-04 opening figure callout wording and the D-05 decision-tree closing wording)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md §"Bibliography pattern" (lines 95-112 give verbatim two-column thebibliography to copy)
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md §"Headline Finding" (lines 19-38 give the 3 sub-findings that motivate the paper)
    - v7.tex lines 74-92 (two-column multicol bibliography — copy structure)
    - results/compare/workload_characterization.md (numbers for the opening-paragraph α_raw=0.23 / 1.03 / 462KB outlier name-drops)
  </read_first>
  <action>
    **Create docs/sections/01-intro.tex (~350 words, ~15-20 lines of prose):**

    Open with the D-01 surprise-finding hook. Frame: the paper ran the same 6 policies on 2 public-records traces (Congress.gov + CourtListener); the policies rank identically on both workloads EXCEPT W-TinyLFU and SIEVE swap — tied on Congress, separated by 4-5pp on Court. Caption of Figure 1 (already on page 1) carries the visual; this section's job is to tell the reader: (a) this paper explains why, (b) the answer is not "W-TinyLFU is better" — it's about workload structure, (c) the rest of the paper unpacks this mechanism. Close the section by listing 4 concrete contributions: (i) two real legislative workloads characterized side-by-side, (ii) 5-seed Welch's-t-backed policy comparison with CI bands, (iii) SIEVE-vs-W-TinyLFU mechanism explanation grounded in raw α × size distribution, (iv) practitioner decision tree. Use first-person for methodology-choice paragraphs per D-02 ("We chose replay-Zipf because..."), third-person for results.

    Must include these specific phrases/numbers (from 05-ANALYSIS.md Headline Finding + workload_characterization.md):
    - "W-TinyLFU dominates the miss-ratio dimension in 11 of 12 cells"
    - "raw $\\alpha = 0.23$ on Congress vs.\\ $\\alpha = 1.03$ on Court"
    - "single 462\\,KB outlier object" (the byte-MRC foreshadowing)
    - Figure 1 cross-reference: "Figure~\\ref{fig:opening}"

    Use first-person for "We" framing ("We compare six eviction policies..."). No section titles above H1 — the section starts with "\\section{Introduction}".

    **Create docs/sections/bibliography.tex with the two-column thebibliography (copy shape from v7.tex:74-92, keys from PATTERNS.md):**

    ```latex
    \begingroup
    \footnotesize
    \setlength{\parskip}{0pt}
    \begin{multicols}{2}
    \begin{thebibliography}{99}
    \setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}
    \bibitem{zhang2024sieve} Y.~Zhang et al., SIEVE is Simpler than LRU, \emph{NSDI} '24.
    \bibitem{yang2023fifo}   J.~Yang et al., FIFO Queues are All You Need, \emph{SOSP} '23.
    \bibitem{waldspurger2015shards} C.~Waldspurger et al., SHARDS, \emph{FAST} '15.
    \bibitem{einziger2017tinylfu}  G.~Einziger et al., TinyLFU, \emph{ACM ToS} '17.
    \bibitem{caffeine2024}   B.~Manes, Caffeine v3.1.8 reference implementation.
    \bibitem{clauset2009powerlaw} A.~Clauset et al., Power-Law Distributions in Empirical Data, \emph{SIAM Rev} '09.
    \end{thebibliography}
    \end{multicols}
    \endgroup
    ```

    Plans 04 + 05 may \\cite{} these keys from their section bodies.
  </action>
  <verify>
    <automated>test -f docs/sections/01-intro.tex && test -f docs/sections/bibliography.tex && grep -c "\\\\bibitem" docs/sections/bibliography.tex && grep -q "\\\\section{Introduction}" docs/sections/01-intro.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/01-intro.tex` exit 0
    - `test -f docs/sections/bibliography.tex` exit 0
    - `grep -c '\\bibitem' docs/sections/bibliography.tex` returns 6 (the 6 canonical citations)
    - `grep -q 'zhang2024sieve' docs/sections/bibliography.tex && grep -q 'yang2023fifo' docs/sections/bibliography.tex && grep -q 'einziger2017tinylfu' docs/sections/bibliography.tex && grep -q 'caffeine2024' docs/sections/bibliography.tex && grep -q 'waldspurger2015shards' docs/sections/bibliography.tex && grep -q 'clauset2009powerlaw' docs/sections/bibliography.tex` exit 0
    - `grep -q '\\section{Introduction}' docs/sections/01-intro.tex` exit 0
    - `grep -q '11 of 12' docs/sections/01-intro.tex` exit 0 (must carry the headline finding)
    - `grep -q '462' docs/sections/01-intro.tex` exit 0 (must foreshadow the byte-MRC outlier)
    - `grep -q 'fig:opening' docs/sections/01-intro.tex` exit 0 (must cross-reference Figure 1)
    - Intro section word count > 200: `wc -w docs/sections/01-intro.tex` > 200
  </acceptance_criteria>
  <done>Intro section and bibliography exist; intro names the headline finding and cross-references Figure 1; bibliography has 6 bibitems with the exact keys Plans 04/05 will \\cite.</done>
</task>

<task type="auto">
  <name>Task 3: Add `paper` target to Makefile and `docs/*.pdf` handling to .gitignore</name>
  <files>Makefile, .gitignore</files>
  <read_first>
    - Makefile (current contents — confirm .PHONY line 13 and target-indent convention using tab)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md §"Build command" (lines 119-128 give the `cd docs && latexmk -pdf DOC-02-final-report.tex` recipe) and §"Constraints Worth Flagging" (lines 420-431 discuss docs/*.pdf decision)
    - .gitignore lines 50-55 (existing LaTeX artifact handling — confirm we're adding to the LaTeX block, not a new block)
  </read_first>
  <action>
    **Edit Makefile to add a `paper` target:**

    1. Add `paper` to the .PHONY line (line 13) — append to the end of the existing list: `.PHONY: all clean run run-sweep plots test shards-large phase-04 ablation-s3fifo ablation-sieve ablation-doorkeeper paper`
    2. Append a new target at the END of the Makefile (after the `phase-04` target), using tab-indented shell commands:

    ```make
    # ==================== Phase 6 — Paper build (DOC-02) ====================
    # Builds docs/DOC-02-final-report.pdf via latexmk. Assumes all section .tex
    # files under docs/sections/ exist (Plans 04 + 05 provide them). Runs
    # inside docs/ so \includegraphics paths like ../results/... resolve.
    paper:
    	cd docs && latexmk -pdf DOC-02-final-report.tex
    ```

    **Edit .gitignore to prevent accidentally committing the built PDF:**

    Append after the existing LaTeX-artifact block (after line 55 `progress.log`):
    ```
    # Phase 6 LaTeX build artifacts (docs/ subdirectory)
    docs/*.aux
    docs/*.fdb_latexmk
    docs/*.fls
    docs/*.log
    docs/*.out
    docs/*.synctex.gz
    # Rendered paper PDF — regenerable via `make paper`; uncomment to commit
    # docs/DOC-02-final-report.pdf
    ```

    Rationale: the PDF is a build artifact, not source; it's regenerable. If user wants to commit the final submission PDF at the end, they can uncomment or force-add. `docs/demo-backup.mov` handling is deferred to Plan 07 (demo-recording plan).
  </action>
  <verify>
    <automated>grep -q "^paper:" Makefile && grep -q "latexmk -pdf DOC-02-final-report" Makefile && grep -q "^docs/\\*\\.aux" .gitignore && make -n paper 2>&1 | grep -q "latexmk"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^paper:' Makefile` exit 0 (target exists as a first-column rule)
    - `grep -q 'cd docs && latexmk -pdf DOC-02-final-report.tex' Makefile` exit 0 (exact recipe per PATTERNS.md)
    - `grep -q '^\\.PHONY:.*paper' Makefile` exit 0 (paper is declared PHONY)
    - `make -n paper 2>&1 | grep -q 'latexmk'` exit 0 (make dry-run shows the latexmk invocation)
    - `grep -q '^docs/\\*\\.aux' .gitignore` exit 0 (LaTeX artifacts ignored)
    - `grep -q '^docs/\\*\\.fdb_latexmk' .gitignore` exit 0
    - `grep -q '^docs/\\*\\.synctex\\.gz' .gitignore` exit 0
  </acceptance_criteria>
  <done>`make paper` recipe exists and will invoke latexmk in docs/; LaTeX build artifacts are gitignored under docs/; the final PDF line is present but commented so the user can uncomment at submission time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 6 Plan 01 introduces no new trust boundaries. All changes are to
repository source files (.tex, Makefile, .gitignore) authored by the
developer. No new input surface, no network calls, no parser of
untrusted data, no privilege change. The `latexmk` binary invoked by
`make paper` is a standard development tool already assumed present.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | `docs/*.tex` contents | accept | File-authoring-only change; no runtime data surface; diff review at commit time is sufficient |
| T-06-01-02 | Information Disclosure | `.env` leakage into `demo.sh` ecosystem | n/a (this plan) | Deferred to Plan 02 which authors demo.sh |
</threat_model>

<verification>
Phase-level checks this plan enables (gated by Plans 04-06 filling the sections):
- `make paper` will exit 0 once all `docs/sections/*.tex` files land
- `docs/DOC-02-final-report.pdf` will be produced with page count > 0
</verification>

<success_criteria>
Plan 01 is complete when:
- docs/DOC-02-final-report.tex exists with preamble, title, page-1 figure hook, and 10 \input{} stubs
- docs/sections/01-intro.tex exists with the D-01 surprise-finding hook and Figure 1 cross-reference
- docs/sections/bibliography.tex exists with 6 bibitems using exact keys from PATTERNS.md
- Makefile has a `paper` target that invokes `latexmk -pdf` from docs/
- .gitignore handles LaTeX build artifacts under docs/
- All three tasks' automated acceptance criteria pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-writeup-demo/06-01-SUMMARY.md`
documenting: the exact \input{} chain Plans 04 + 05 must honor; any deviations
from PATTERNS.md; and a note that `make paper` will fail until section files
land (this is expected).
</output>
