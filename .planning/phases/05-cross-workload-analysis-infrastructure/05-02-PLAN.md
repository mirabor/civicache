---
phase: 05-cross-workload-analysis-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - results/congress/workload_stats.json
autonomous: true
requirements:
  - ANAL-03
canonical_refs:
  - .planning/phases/05-cross-workload-analysis-infrastructure/05-CONTEXT.md
  - .planning/phases/05-cross-workload-analysis-infrastructure/05-PATTERNS.md
tags:
  - workload-stats
  - data-regeneration
  - anal-03-foundation
must_haves:
  truths:
    - "`results/congress/workload_stats.json` exists after this plan runs"
    - "The JSON contains all 10 keys required by ANAL-03: trace_path, total_requests, unique_objects, alpha_mle, ohw_ratio, mean_size, median_size, p95_size, max_size, working_set_bytes"
    - "`total_requests` in the JSON equals `wc -l traces/congress_trace.csv - 1` (header row subtracted)"
    - "`trace_path` value is exactly the string `traces/congress_trace.csv` (not an absolute path)"
    - "Both workloads now have the file: `ls results/{congress,court}/workload_stats.json` shows both files exist"
    - "The Congress `alpha_mle` value is in a plausible Zipf range [0.5, 1.3] (Phase 1 MLE estimate was 0.797; regenerated value should be within ~±0.05 of that)"
    - "`python3 -c \"import json; d=json.load(open('results/congress/workload_stats.json')); assert set(d.keys()) == {'trace_path','total_requests','unique_objects','alpha_mle','ohw_ratio','mean_size','median_size','p95_size','max_size','working_set_bytes'}\"` exits 0"
  artifacts:
    - path: "results/congress/workload_stats.json"
      provides: "Congress workload characterization — feeds ANAL-03 side-by-side characterization table in Plan 05-06"
      contains: "trace_path"
  key_links:
    - from: "traces/congress_trace.csv"
      to: "results/congress/workload_stats.json"
      via: "existing `scripts/workload_stats_json.py` (Phase 3 Plan 03 output; 127 lines, no modifications needed)"
      pattern: "workload_stats_json\\.py"
    - from: "results/congress/workload_stats.json"
      to: "Plan 05-06 ANAL-03 characterization table"
      via: "Plan 05-06 will read both `results/{congress,court}/workload_stats.json` and emit `results/compare/workload_characterization.{md,json}`"
      pattern: "workload_stats.json"
---

<objective>
Regenerate the missing Congress workload_stats.json artifact — a one-shot invocation of the existing `scripts/workload_stats_json.py` (built in Phase 3 Plan 03) against `traces/congress_trace.csv`. Phase 3 ran the script for Court but not Congress; ANAL-03 requires BOTH workloads' stats to build its side-by-side characterization table.

**Why this is its own plan (rather than a task inside 05-06):** Per D-07 in 05-CONTEXT.md, it's a Wave 1 prerequisite — Plan 05-06 (characterization + regime tables) can't start until both workload_stats.json files exist. Running it in parallel with Plan 05-01 (`--seed` flag, also Wave 1 and touches disjoint files) maximizes parallelism and minimizes wall-clock.

**Why no code changes are needed:** `scripts/workload_stats_json.py` already exists (127 lines, Phase 3 Plan 03 commit 2136cd7). It emits exactly the 10 keys ANAL-03 consumes: `trace_path, total_requests, unique_objects, alpha_mle, ohw_ratio, mean_size, median_size, p95_size, max_size, working_set_bytes`. The Court file at `results/court/workload_stats.json` was generated by this same script and serves as the schema reference.

**Back-compat note (D-15):** `results/` is gitignored, so this file is a regenerable artifact (not committed). The regeneration is idempotent — running the same command twice produces byte-identical output (the estimator is deterministic).

**Purpose:** Closes the D-07 data gap flagged in 05-CONTEXT.md. Feeds directly into Plan 05-06's ANAL-03 characterization table, which needs both workloads' rows populated to be non-degenerate.

**Output:**
- A single JSON file at `results/congress/workload_stats.json` (~300 bytes, 10 keys) mirroring the schema of `results/court/workload_stats.json`.
- No code changes. No script modifications. No Makefile updates.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-workload-analysis-infrastructure/05-CONTEXT.md
@.planning/phases/05-cross-workload-analysis-infrastructure/05-PATTERNS.md

# Existing script we invoke — NO edits
@scripts/workload_stats_json.py

# Schema reference — the Court file's shape is what the Congress file must match
@results/court/workload_stats.json

<interfaces>
<!-- The script's invocation contract + output schema — extracted so the executor needs no exploration. -->

From scripts/workload_stats_json.py (existing, NO CHANGES):
```
Usage:
    python3 scripts/workload_stats_json.py --trace <in.csv> --output <out.json>

Required args:
    --trace   input trace CSV (must have columns: timestamp, key, size)
    --output  output JSON path (parent directory is auto-created)
```

From results/court/workload_stats.json (reference schema — the Congress output MUST have these 10 keys):
```json
{
  "trace_path": "traces/court_trace.csv",
  "total_requests": 20000,
  "unique_objects": 15018,
  "alpha_mle": 1.0278851467973213,
  "ohw_ratio": 1.0,
  "mean_size": 3144.6356,
  "median_size": 1381,
  "p95_size": 6221,
  "max_size": 462490,
  "working_set_bytes": 59650083
}
```

From Phase 1 MLE reference (existing result on Congress — the new regenerated value should match this within floating-point precision, since the estimator is deterministic):
- The Phase 1 progress report + STATE.md cite `α_mle ≈ 0.797` for Congress.
- Congress trace has 20,692 total requests per Phase 1 TRACE-01 validation.
</interfaces>
</context>

<threat_model>
**Trust boundary scope:** Pure local-process data regeneration. No network, no untrusted input.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | results/congress/workload_stats.json is a local file under a gitignored directory | accept | No sensitive data in the trace or stats — Congress.gov bill/amendment/vote IDs are public; object sizes are byte counts. D-15 keeps results/ out of git. No PII. |
| T-05-02-02 | Data integrity / schema drift | workload_stats_json.py could silently emit a different key-set than results/court/workload_stats.json | mitigate | Task verifies the emitted JSON's key-set matches the 10-key reference exactly (set-equality check in the acceptance criteria). If the script has drifted (it shouldn't — no edits since Phase 3 Plan 03), the verification catches it. |
| T-05-02-03 | Reproducibility | Regenerating this file in the future must be a no-op (same output every time) | mitigate | The estimator is deterministic (Newton's method with fixed init alpha=1.0 and fixed trace iteration order). Two invocations produce byte-identical JSON. Acceptance criteria include re-running the command and diffing to confirm idempotency. |

**Block on:** none. All threats LOW. This is a non-destructive data regeneration task.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run workload_stats_json.py against traces/congress_trace.csv and verify output schema matches the Court reference</name>
  <files>results/congress/workload_stats.json</files>
  <read_first>
    - scripts/workload_stats_json.py (127 lines — confirm current behavior before invoking; verify the script still reads --trace/--output as positional kwargs and writes 10 keys)
    - results/court/workload_stats.json (10-key reference — the Congress output must have the same 10 keys)
    - traces/congress_trace.csv (first 5 lines only, via `head -5`, to confirm it still has the 3-column `timestamp,key,size` header the script requires at line 87-91 of workload_stats_json.py)
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-CONTEXT.md (D-07 missing Congress workload_stats.json gap + D-13 Phase 3 schema lock)
  </read_first>
  <action>
**(a) Verify prerequisites before invocation.**

1. Confirm `traces/congress_trace.csv` exists:
   ```
   test -f traces/congress_trace.csv && wc -l traces/congress_trace.csv
   ```
   Expected: file exists, line count > 20000 (Phase 1 collected 20,692 requests).

2. Confirm the trace CSV header is exactly `timestamp,key,size`:
   ```
   head -1 traces/congress_trace.csv
   ```
   Expected output (exact match): `timestamp,key,size`

3. Confirm `scripts/workload_stats_json.py` is unchanged since Phase 3 Plan 03 (no unexpected edits):
   ```
   wc -l scripts/workload_stats_json.py
   ```
   Expected: 127 lines. If different, read the full script before proceeding — the output schema may have drifted.

4. Confirm `results/court/workload_stats.json` still has the 10 reference keys (so the set-equality check later has a known-good baseline):
   ```
   python3 -c "import json; d = json.load(open('results/court/workload_stats.json')); print(sorted(d.keys()))"
   ```
   Expected (exact sorted list): `['alpha_mle', 'max_size', 'mean_size', 'median_size', 'ohw_ratio', 'p95_size', 'total_requests', 'trace_path', 'unique_objects', 'working_set_bytes']`

**(b) Invoke the script.** Run the Phase 3 Plan 03 idiom exactly as documented in 05-CONTEXT.md D-07:

```bash
python3 scripts/workload_stats_json.py \
    --trace traces/congress_trace.csv \
    --output results/congress/workload_stats.json
```

The script prints a one-line summary `Wrote results/congress/workload_stats.json: {stats_dict}` on success. Capture the full stdout for the SUMMARY.md.

Runtime expectation: ~2-5 seconds (Newton's method MLE on ~2000 Zipf ranks + one linear pass for sizes/OHW). If it takes longer than 30 seconds, abort and read the script to diagnose (indicates either a trace-format regression or a script edit that broke something).

**(c) Verify the output.** Immediately after the invocation:

1. Confirm the file exists:
   ```
   ls -la results/congress/workload_stats.json
   ```

2. Confirm it's valid JSON with exactly the 10 reference keys:
   ```
   python3 -c "
   import json
   d = json.load(open('results/congress/workload_stats.json'))
   reference_keys = {'trace_path', 'total_requests', 'unique_objects', 'alpha_mle',
                     'ohw_ratio', 'mean_size', 'median_size', 'p95_size',
                     'max_size', 'working_set_bytes'}
   assert set(d.keys()) == reference_keys, f'Key mismatch: {set(d.keys()) ^ reference_keys}'
   print('Schema OK:', sorted(d.keys()))
   "
   ```

3. Confirm Congress-specific sanity values:
   ```
   python3 -c "
   import json
   d = json.load(open('results/congress/workload_stats.json'))
   assert d['trace_path'] == 'traces/congress_trace.csv', f'trace_path wrong: {d[\"trace_path\"]}'
   assert 0.5 <= d['alpha_mle'] <= 1.3, f'alpha_mle {d[\"alpha_mle\"]} outside plausible Zipf range [0.5, 1.3]'
   assert d['total_requests'] > 20000, f'total_requests {d[\"total_requests\"]} suspiciously low'
   assert d['unique_objects'] > 0
   assert d['working_set_bytes'] > 0
   print('Sanity OK:', {k: d[k] for k in ('trace_path', 'total_requests', 'unique_objects', 'alpha_mle', 'ohw_ratio')})
   "
   ```

**(d) Idempotency check.** Re-run the exact command a second time and confirm byte-identical output:

```
python3 scripts/workload_stats_json.py --trace traces/congress_trace.csv --output /tmp/_ws2.json
diff results/congress/workload_stats.json /tmp/_ws2.json
echo "Idempotent: exit $?"
rm /tmp/_ws2.json
```

Expected: `diff` exits 0 (files identical). If they differ, the estimator is non-deterministic — flag this as a plan deviation.

**(e) Cross-reference with Court file.** Confirm both files now exist and have matching key sets:

```
python3 -c "
import json
c = set(json.load(open('results/congress/workload_stats.json')).keys())
k = set(json.load(open('results/court/workload_stats.json')).keys())
assert c == k, f'Key set mismatch between workloads: {c ^ k}'
print('Both workloads have matching 10-key schema')
"
```

**(f) NO edits to scripts/workload_stats_json.py, NO edits to any other file.** This plan is data-regeneration only. If the script turns out to need modification (unexpected), STOP and escalate — that would be outside Plan 05-02's scope and should become a separate plan.
  </action>
  <verify>
    <automated>cd /Users/mirayu/civicache &amp;&amp; test -f traces/congress_trace.csv &amp;&amp; python3 scripts/workload_stats_json.py --trace traces/congress_trace.csv --output results/congress/workload_stats.json &amp;&amp; python3 -c "import json; d=json.load(open('results/congress/workload_stats.json')); ref={'trace_path','total_requests','unique_objects','alpha_mle','ohw_ratio','mean_size','median_size','p95_size','max_size','working_set_bytes'}; assert set(d.keys())==ref, 'SCHEMA FAIL'; assert d['trace_path']=='traces/congress_trace.csv', 'TRACE_PATH FAIL'; assert 0.5&lt;=d['alpha_mle']&lt;=1.3, f'ALPHA_MLE {d[\"alpha_mle\"]} FAIL'; assert d['total_requests']&gt;20000, f'TOTAL_REQUESTS {d[\"total_requests\"]} FAIL'; print('ALL CHECKS PASS:', sorted(d.keys()))" &amp;&amp; python3 scripts/workload_stats_json.py --trace traces/congress_trace.csv --output /tmp/_ws2.json &amp;&amp; diff results/congress/workload_stats.json /tmp/_ws2.json &amp;&amp; echo "IDEMPOTENT OK" &amp;&amp; rm /tmp/_ws2.json &amp;&amp; python3 -c "import json; c=set(json.load(open('results/congress/workload_stats.json')).keys()); k=set(json.load(open('results/court/workload_stats.json')).keys()); assert c==k, 'CROSS-WL KEY MISMATCH'; print('BOTH WORKLOADS OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `results/congress/workload_stats.json` exists and is a valid JSON document
    - The 10 required keys are present: `trace_path, total_requests, unique_objects, alpha_mle, ohw_ratio, mean_size, median_size, p95_size, max_size, working_set_bytes` (set-equality with the reference 10-key set — no extras, no missing)
    - `trace_path` value is exactly the string `"traces/congress_trace.csv"` (not an absolute path)
    - `alpha_mle` value is in the plausible range `[0.5, 1.3]` (Phase 1 baseline was ~0.797; regeneration should land within ~±0.05)
    - `total_requests` is > 20000 (Phase 1 collected 20,692; the script's line count — including header — minus 1 should equal `total_requests`)
    - `unique_objects` > 0
    - `working_set_bytes` > 0
    - `ohw_ratio` is in `[0.0, 1.0]`
    - Idempotency: running the script a second time produces byte-identical output (diff exits 0)
    - Cross-workload schema consistency: `results/congress/workload_stats.json` and `results/court/workload_stats.json` have identical key sets
    - No code files modified: `git diff --stat scripts/workload_stats_json.py` shows no changes; `git diff --stat src/` shows no changes; `git diff --stat include/` shows no changes; `git diff --stat Makefile` shows no changes
    - `results/congress/workload_stats.json` is under a gitignored directory (D-15 — confirm via `git check-ignore results/congress/workload_stats.json` which should exit 0)
  </acceptance_criteria>
  <done>
    `results/congress/workload_stats.json` exists with the 10-key ANAL-03 schema, `trace_path` references the correct Congress trace, `alpha_mle` is in the plausible range matching Phase 1's ~0.797 estimate, both Congress and Court workload_stats.json files share identical key sets, idempotency verified (re-running produces byte-identical output), and no code files were touched.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

1. **Both workload_stats.json files exist:** `ls results/{congress,court}/workload_stats.json` lists both.
2. **Schemas match:** Python set-equality check on the two files' keys returns True.
3. **Congress numbers plausible:** `alpha_mle` ∈ [0.5, 1.3], `total_requests` > 20000, `unique_objects` > 0.
4. **Idempotent:** regenerating to a temp path and diffing against the canonical path exits 0.
5. **Ready for Plan 05-06:** Plan 05-06's ANAL-03 characterization table can now read both `results/{congress,court}/workload_stats.json` and emit a non-degenerate side-by-side table.
</verification>

<success_criteria>
ANAL-03 foundation verified: Congress workload_stats.json matches Court schema; Plan 05-06 has both workloads' characterization data available to build the side-by-side table. D-07 gap closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-workload-analysis-infrastructure/05-02-SUMMARY.md` per template summarizing:
- The exact JSON emitted (paste the 10-key file contents verbatim for future archival)
- The Congress-specific values of interest: `alpha_mle`, `ohw_ratio`, `unique_objects`, `total_requests`, `mean_size`, `median_size`, `p95_size`, `max_size`, `working_set_bytes`
- Side-by-side summary comparison with Court: any interesting contrast (e.g., "Congress α≈0.8 vs Court α≈1.03 — Court has higher skew; Congress 20,692 vs Court 20,000 requests; Congress mean size vs Court mean size")
- Idempotency confirmation (exit 0 from the diff)
- Any deviation (e.g., if the script needed an unexpected flag, or the alpha_mle landed outside the plausible range)
</output>
