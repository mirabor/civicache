---
phase: 06-writeup-demo
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - docs/sections/02-related.tex
  - docs/sections/03-workload.tex
  - docs/sections/04-policies.tex
  - docs/sections/06-shards.tex
  - docs/sections/07-ablations.tex
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "Five body section .tex files land under docs/sections/ — each compiles cleanly when the main .tex input{}'s it"
    - "§3 Workload Characterization carries the 10-row table (Congress vs Court) verbatim from results/compare/workload_characterization.md"
    - "§4 Policy Comparison cites Figure ref{fig:opening} (compare_mrc_2panel.pdf) with the ±1σ band explanation and includes per-workload alpha_sensitivity figures (D-13)"
    - "§6 SHARDS Validation carries the 4-rate table (0.01%/0.1%/1%/10%) plus shards_mrc_overlay figure"
    - "§7 Ablations carries all 3 ablation figures (doorkeeper, s3fifo, sieve) with named findings from Phase 4 SUMMARYs"
    - "Every significance claim in body text cites the aggregated_csv's p_value column OR is hedged as 'single-seed' for the Mixed Sizes outlier"
  artifacts:
    - path: "docs/sections/02-related.tex"
      provides: "Related Work — 150-200 words, cites SIEVE/S3-FIFO/TinyLFU/SHARDS papers"
      min_lines: 8
    - path: "docs/sections/03-workload.tex"
      provides: "Workload Characterization — 400-500 words plus 10-row table (Congress vs Court) from workload_characterization.md"
      min_lines: 40
    - path: "docs/sections/04-policies.tex"
      provides: "Policy Comparison — 500-600 words, figure cites, policy-by-policy narrative from 05-ANALYSIS.md Why W-TinyLFU Wins 11 of 12 MRC Cells"
      min_lines: 50
    - path: "docs/sections/06-shards.tex"
      provides: "SHARDS Validation — 300-400 words plus 4-rate self-convergence table plus shards_mrc_overlay figure"
      min_lines: 35
    - path: "docs/sections/07-ablations.tex"
      provides: "Ablations section — 3 ablation figures plus named findings per D-14 (all 3 live together)"
      min_lines: 50
  key_links:
    - from: "docs/sections/03-workload.tex"
      to: "results/compare/workload_characterization.md"
      via: "10-row tabularx with Congress vs Court columns"
      pattern: "0.231|Zipf.*MLE|tabularx"
    - from: "docs/sections/04-policies.tex"
      to: "results/compare/figures/compare_mrc_2panel.pdf"
      via: "ref{fig:opening} reference"
      pattern: "fig:opening|compare_mrc_2panel"
    - from: "docs/sections/06-shards.tex"
      to: "results/shards_large/figures/shards_mrc_overlay.pdf"
      via: "includegraphics plus label{fig:shards_overlay}"
      pattern: "shards_mrc_overlay"
    - from: "docs/sections/07-ablations.tex"
      to: "results/{congress,court}/figures/ablation_{doorkeeper,s3fifo,sieve}.pdf"
      via: "3 separate includegraphics blocks (one per ablation axis)"
      pattern: "ablation_(doorkeeper|s3fifo|sieve)"
---

<objective>
Author the paper body sections — Related Work (§2), Workload Characterization
(§3), Policy Comparison (§4), SHARDS Validation (§6), and Ablations (§7) — as
modular .tex files under docs/sections/ included by the Plan 01 main .tex.
These are the "breadth" sections per D-03 (paragraph-length treatment for
everything except the §5 mechanism deep-dive which is Plan 05's job).

Purpose: Closes 4 of the 5 bullet points of DOC-02 SC-1 (workload
characterization table, policy comparison with full MRC + error bands, SHARDS
4-rate table, ablation figures). §5 mechanism deep-dive and §8-9 (byte-MRC +
conclusion + decision tree) are Plan 05. §1 intro + bibliography are Plan 01.

Output: Five docs/sections/*.tex files that compile cleanly when included by
docs/DOC-02-final-report.tex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-writeup-demo/06-CONTEXT.md
@.planning/phases/06-writeup-demo/06-PATTERNS.md

<!-- Primary prose source — already structured with paper-section mapping -->
@.planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md

<!-- Data sources -->
@results/compare/workload_characterization.md

<!-- Phase 4 findings narrated in STATE.md per-phase decisions -->
@.planning/STATE.md

<!-- Preamble + tabular + figure patterns established by Plan 01 -->
@progress.tex

<interfaces>
Canonical figure paths (all figures already rendered — cite by relative path):
§4: ../results/compare/figures/compare_mrc_2panel.pdf        (cross-workload MRC with 1σ bands)
§4: ../results/compare/figures/compare_policy_delta.pdf      (optional secondary)
§4: ../results/{congress,court}/figures/alpha_sensitivity.pdf (D-13 2nd-tier — per-workload alpha sweep)
§6: ../results/shards_large/figures/shards_mrc_overlay.pdf   (SHARDS overlay; money shot)
§6: ../results/shards_large/figures/shards_convergence.pdf   (error vs sampling rate)
§7: ../results/{congress,court}/figures/ablation_doorkeeper.pdf
§7: ../results/{congress,court}/figures/ablation_s3fifo.pdf
§7: ../results/{congress,court}/figures/ablation_sieve.pdf

Bibliography cite keys (established in Plan 01 bibliography.tex):
zhang2024sieve yang2023fifo waldspurger2015shards einziger2017tinylfu caffeine2024 clauset2009powerlaw

Workload characterization table (numbers must match workload_characterization.md verbatim):
Total requests      Congress=20,692     Court=20,000
Unique objects      Congress=18,970     Court=15,018
Zipf α (MLE)        Congress=0.231      Court=1.028
OHW ratio (10% win) Congress=0.989      Court=1.000
Mean size (bytes)   Congress=751.9      Court=3,144.6
Median size         Congress=231        Court=1,381
p95 size            Congress=2,698      Court=6,221
Max size            Congress=6,700      Court=462,490
Working set bytes   Congress=14,490,463 Court=59,650,083

Phase 4 ablation findings (from STATE.md per-phase decisions — reproduce, do NOT invent):
- Doorkeeper: context-dependent with gap at alpha extremes; -0.72pp peak win at Court α=1.1; +0.52pp slight loss at Court α=0.6; Congress within ±0.22pp noise — marginal hedge per Einziger-Friedman §4.3
- S3-FIFO ratio: smaller small-queue monotonically wins; 6.3pp gap at Court α=1.2 vs 1.2pp Congress — publishable result against Yang et al. (SOSP'23) 10% default
- SIEVE no-promotion: SIEVE-NoProm monotonically loses; gap peaks +15.4pp Congress α=1.0 and +11.0pp Court α=1.1 — empirically confirms Zhang et al. (NSDI'24) lazy-promotion claim
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write §2 Related Work + §3 Workload Characterization (with 10-row table)</name>
  <files>docs/sections/02-related.tex, docs/sections/03-workload.tex</files>
  <read_first>
    - results/compare/workload_characterization.md (source of truth for the 10-row table — numbers MUST match verbatim)
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md lines 42-68 (prose source for the "raw α differs" narrative)
    - progress.tex lines 49-62 (tabularx + booktabs pattern to copy)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md lines 82-93 (Tabular pattern — exact tabular idiom)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-02 (hybrid tone — first-person for methodology, third-person for results)
  </read_first>
  <action>
    Create docs/sections/02-related.tex (~150-200 words, ~10 lines of prose). The section starts with `\section{Related Work}` and describes: (1) FIFO-based evictions S3-FIFO and SIEVE and their claim of matching/beating LRU on skewed workloads, (2) TinyLFU-family admission filtering with Caffeine as the reference implementation, (3) SHARDS for efficient MRC construction, (4) our contribution as orthogonal — comparing 6 policies on 2 real public-records workloads with multi-seed CI. MUST use `\cite{zhang2024sieve}`, `\cite{yang2023fifo}`, `\cite{einziger2017tinylfu}`, `\cite{caffeine2024}`, `\cite{waldspurger2015shards}` — at least 4 of these 5 keys.

    Create docs/sections/03-workload.tex (~400-500 words, ~40 lines). Structure:

    1. `\section{Workload Characterization}\label{sec:workload}` + 1-sentence opening.
    2. Subsection "Trace collection" (~150 words) — 1 paragraph per workload. Congress.gov paragraph pulls from PROCESS.md:99-112 (20,692 requests, 1.2s base delay with backoff and jitter, bill/amendment/vote endpoints). Court paragraph pulls from scripts/collect_court_trace.py narrative (20,000 requests, 0.8s+0.4s jitter, 80/20 metadata/full-text mix, CourtListener REST v4, ~9h overnight collection). Use first-person for methodology choices per D-02: "We chose CourtListener REST v4 because PACER's \\$500--\\$2000 paywall made a 20K trace infeasible."
    3. Subsection "Trace characteristics" — the 10-row table. Copy numbers VERBATIM from results/compare/workload_characterization.md. Use the tabularx pattern from progress.tex:49-62 with `\footnotesize` and `\renewcommand{\arraystretch}{0.92}`. The table:

    ```latex
    \begin{table}[h]
    \centering
    \footnotesize
    \renewcommand{\arraystretch}{0.92}
    \begin{tabularx}{\textwidth}{@{}lXX@{}}
    \toprule
    \textbf{Metric} & \textbf{Congress} & \textbf{Court} \\
    \midrule
    Total requests        & 20{,}692       & 20{,}000 \\
    Unique objects        & 18{,}970       & 15{,}018 \\
    Zipf $\alpha$ (MLE)   & 0.231          & 1.028 \\
    OHW ratio (10\% win)  & 0.989          & 1.000 \\
    Mean size (bytes)     & 751.9          & 3{,}144.6 \\
    Median size (bytes)   & 231            & 1{,}381 \\
    p95 size (bytes)      & 2{,}698        & 6{,}221 \\
    Max size (bytes)      & 6{,}700        & 462{,}490 \\
    Working set (bytes)   & 14{,}490{,}463 & 59{,}650{,}083 \\
    \bottomrule
    \end{tabularx}
    \caption{Workload characterization --- Congress.gov vs.\ CourtListener raw traces.
    \textbf{Note the 335$\times$ max/median ratio on Court: a single 462\,KB
    document dominates the byte-miss-ratio analysis in \S\ref{sec:byte-mrc}.}}
    \label{tab:workload}
    \end{table}
    ```

    4. Subsection "Why the raw $\alpha$ differs" (~150 words) — adapted from 05-ANALYSIS.md lines 54-61. Congress.gov API calls are client-generated random queries so α=0.23 matches "essentially uniform random over the key space"; CourtListener serves judicial documents that get cited in briefs, dockets that get refreshed during active cases, same cluster-IDs appearing across search results → real structural locality.
    5. Subsection "Why we still run replay-Zipf on both" (~100 words) — adapted from 05-ANALYSIS.md lines 63-68. Both traces are OHW-dominated (0.989 and 1.000), so neither gives meaningful policy-comparison results without a popularity overlay. Replay-Zipf preserves real keys and sizes while imposing a controlled α sweep. Use first-person per D-02.

    Cross-references: use `\label{tab:workload}` (§5 will reference) and forward-reference `\S\ref{sec:byte-mrc}` (Plan 05 defines in 08-byte-mrc.tex).
  </action>
  <verify>
    <automated>test -f docs/sections/02-related.tex && test -f docs/sections/03-workload.tex && grep -c 'cite{' docs/sections/02-related.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/02-related.tex` exit 0
    - `test -f docs/sections/03-workload.tex` exit 0
    - `grep -q 'section{Related Work}' docs/sections/02-related.tex` exit 0
    - `grep -c 'cite{' docs/sections/02-related.tex` returns value >= 4 (four or more citations)
    - `grep -q 'section{Workload Characterization}' docs/sections/03-workload.tex` exit 0
    - `grep -q 'label{tab:workload}' docs/sections/03-workload.tex` exit 0
    - `grep -q 'tabularx' docs/sections/03-workload.tex` exit 0
    - `grep -q '0.231' docs/sections/03-workload.tex` exit 0 (Congress α_raw)
    - `grep -q '1.028' docs/sections/03-workload.tex` exit 0 (Court α_raw)
    - `grep -q '462' docs/sections/03-workload.tex` exit 0 (max size outlier)
    - `grep -q '0.989' docs/sections/03-workload.tex` exit 0 (Congress OHW)
    - `grep -q 'replay-Zipf\|replay.zipf' docs/sections/03-workload.tex` exit 0
    - `wc -w < docs/sections/02-related.tex` returns value > 80
    - `wc -w < docs/sections/03-workload.tex` returns value > 300
  </acceptance_criteria>
  <done>§2 and §3 exist with the required elements; §3 carries the 10-row table VERBATIM from workload_characterization.md with the correct label; the "raw α differs" mechanism paragraph is in place as foundation for §5.</done>
</task>

<task type="auto">
  <name>Task 2: Write §4 Policy Comparison (headline result + per-workload alpha sensitivity + robustness finding)</name>
  <files>docs/sections/04-policies.tex</files>
  <read_first>
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md lines 71-95 ("Why W-TinyLFU Wins 11/12 MRC Cells"), lines 219-242 ("Why Small-Cache Favors W-TinyLFU Even More"), lines 287-307 ("Policies Are Robust Across Workloads")
    - results/compare/winner_per_regime.md (Small Cache and OHW Regime rows give concrete W-TinyLFU vs LRU gaps)
    - results/compare/aggregated/congress/mrc_aggregated.csv (head + tail — see schema, pull specific numbers if needed)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-04 (compare_mrc_2panel is page-1 figure from Plan 01 — cross-reference as `\ref{fig:opening}`)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-13 (per-workload alpha_sensitivity.pdf is required 2nd-tier figure)
    - .planning/STATE.md Phase 05-04 decision entry (Welch's t-test p values on Congress: p ∈ [1.2e-06, 8.7e-08] at every α in {0.6..1.2})
  </read_first>
  <action>
    Create docs/sections/04-policies.tex (~500-600 words, ~50 lines). Structure:

    1. `\section{Policy Comparison}\label{sec:policies}` + 1-paragraph methodology statement: 5-seed replay-Zipf sweeps across α ∈ {0.6, 0.7, ..., 1.2} and cache fractions {0.001, 0.005, 0.010, 0.020, 0.050, 0.100} on both workloads; Welch's two-sample t-test (scipy.stats.ttest_ind, equal_var=False) against LRU as the reference with p < 0.05 threshold. Cite `\cite{clauset2009powerlaw}` if discussing MLE here.

    2. Subsection "Headline result" (~100 words). First sentence: "W-TinyLFU wins 11 of 12 miss-ratio cells (6 cache fractions × 2 workloads)." Second: the one exception is Congress at cache_frac=0.020 where SIEVE edges W-TinyLFU by less than 1σ. Cross-reference the page-1 figure: "Figure~\ref{fig:opening} (page 1) shows both workloads side-by-side with ±1σ bands across 5 seeds."

    3. Subsection "Why W-TinyLFU dominates" (~200 words) — adapted from 05-ANALYSIS.md lines 73-95. Three bullet mechanisms: (a) 4-bit Count-Min Sketch tracks approximate per-key frequency explicitly, (b) admission filter requires new candidates to out-frequency the current victim, (c) 1% window LRU gives one-hit wonders a chance to prove recurrence. Name where the win is largest (Small Cache Court: W-TinyLFU beats LRU by 12.9pp at cache_frac=0.001 — 0.831 vs 0.960, from winner_per_regime.md) and where it is smallest (Congress cache_frac=0.1 where ~1,900 objects fit — gap shrinks to ~5pp). Cite `\cite{caffeine2024}` for the reference implementation.

    4. Subsection "Per-workload alpha sensitivity" (~150 words + 2-panel figure). Use a side-by-side minipage figure:

    ```latex
    \begin{figure}[h]
    \centering
    \begin{minipage}{0.48\textwidth}
    \includegraphics[width=\textwidth]{../results/congress/figures/alpha_sensitivity.pdf}
    \end{minipage}\hfill
    \begin{minipage}{0.48\textwidth}
    \includegraphics[width=\textwidth]{../results/court/figures/alpha_sensitivity.pdf}
    \end{minipage}
    \caption{Alpha sensitivity at cache\_frac=0.01 --- Congress (left) vs.\ Court (right).}
    \label{fig:alpha_sens}
    \end{figure}
    ```

    Narrative body adapted from Phase 2 Plan 02-06 data: "W-TinyLFU beats LRU monotonically 7.84\%--21.55\% across $\alpha \in \{0.6, \ldots, 1.2\}$ on Congress" and cite Welch's t-test with $p < 10^{-6}$ per STATE.md Plan 05-04 entry ("p ∈ [1.2e-06, 8.7e-08]").

    5. Subsection "Policy ordering is robust across workloads" (~150 words) — adapted from 05-ANALYSIS.md lines 287-307. Congress ordering at cache_frac=0.01: W-TinyLFU ≈ SIEVE > S3-FIFO > CLOCK > LRU > FIFO. Court ordering: identical except W-TinyLFU and SIEVE separate. Forward-reference: "The mechanism behind the SIEVE/W-TinyLFU swap is the subject of §\ref{sec:mechanism}" (Plan 05 defines `sec:mechanism`).

    CRITICAL — numerical claims must be exact:
    - "12.9pp at Court cache_frac=0.001": verify against winner_per_regime.md (W-TinyLFU 0.831, LRU 0.960 → 0.129 → 12.9pp)
    - "7.84% to 21.55% across α ∈ {0.6..1.2}" from Phase 2 Plan 02-06 STATE.md narrative
    - "p < 10^-6" from Phase 5 Plan 05-04 STATE.md narrative — do NOT inflate to 10^-8

    Do not invent numbers not in the source artifacts. If uncertain, hedge.
  </action>
  <verify>
    <automated>test -f docs/sections/04-policies.tex && grep -q 'section{Policy Comparison}' docs/sections/04-policies.tex && grep -q 'fig:opening\|fig:alpha_sens' docs/sections/04-policies.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/04-policies.tex` exit 0
    - `grep -q 'section{Policy Comparison}' docs/sections/04-policies.tex` exit 0
    - `grep -q 'label{sec:policies}' docs/sections/04-policies.tex` exit 0
    - `grep -q '11 of 12\|eleven of twelve' docs/sections/04-policies.tex` exit 0 (headline claim)
    - `grep -q 'ref{fig:opening}' docs/sections/04-policies.tex` exit 0 (page-1 figure cross-ref)
    - `grep -q 'alpha_sensitivity' docs/sections/04-policies.tex` exit 0 (D-13 2nd-tier figs)
    - `grep -q 'label{fig:alpha_sens}' docs/sections/04-policies.tex` exit 0
    - `grep -q 'ref{sec:mechanism}\|sec:mechanism' docs/sections/04-policies.tex` exit 0 (forward-ref to Plan 05)
    - `grep -q '12.9pp\|0.831\|0.960' docs/sections/04-policies.tex` exit 0 (Small Cache numbers from winner_per_regime.md)
    - `grep -q 'Welch\|ttest\|t-test' docs/sections/04-policies.tex` exit 0 (significance methodology named)
    - `wc -w < docs/sections/04-policies.tex` returns value > 400
  </acceptance_criteria>
  <done>§4 exists with headline result, per-workload alpha figure (D-13), robustness narrative, forward-reference to §5; all numeric claims are traceable to workload_characterization.md, winner_per_regime.md, or STATE.md phase-decision entries.</done>
</task>

<task type="auto">
  <name>Task 3: Write §6 SHARDS Validation (4-rate table + shards_mrc_overlay figure)</name>
  <files>docs/sections/06-shards.tex</files>
  <read_first>
    - results/shards_large/shards_convergence.csv (source of truth for the 4-rate self-convergence table: `cat results/shards_large/shards_convergence.csv` — extract MAE and max_abs_error vs reference_rate=0.1)
    - results/shards_large/shards_mrc.csv (head — to confirm which rates are in the 1M regime: {0.0001, 0.001, 0.01, 0.1} per Phase 4 Plan 04-01 STATE.md)
    - .planning/STATE.md Phase 04-01 entry (sanity gate MAE(1%, 10%) = 0.0378 < 0.05; Waldspurger's 0.001 target is on different workload; 50K oracle guard drops rates below 200-sample floor)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-12 (shards_mrc_overlay.pdf is must-have main-body figure)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md lines 82-93 (tabularx pattern)
  </read_first>
  <action>
    Create docs/sections/06-shards.tex (~300-400 words, ~35 lines). Structure:

    1. `\section{SHARDS Validation}\label{sec:shards}` + opening paragraph justifying the rigor work: "No exact oracle exists at 1M-access scale (the O(n · unique_keys) exact-stack-distance algorithm would take hours). We use SHARDS self-convergence: if four sampling rates agree, the sampled MRC is a reliable estimate." Cite `\cite{waldspurger2015shards}`.

    2. Subsection "Methodology" (~100 words). Pulls from Phase 4 Plan 04-01 design: 1M-access synthetic Zipf(α=0.8, 100K objects, seed=42) trace at `traces/shards_large.csv` (gitignored per D-15, regenerable in <5s); 4 sampling rates {0.01%, 0.1%, 1%, 10%}; reference rate for MAE comparison is 10% (highest-fidelity sample per Phase 4 D-02).

    3. Subsection "Self-convergence table" — the 4-rate table. READ results/shards_large/shards_convergence.csv at execute time and fill in the exact MAE and max_abs_error numbers. Schema per STATE.md: reference_rate, compared_rate, mae, max_abs_error, num_points, n_samples_reference, n_samples_compared. Expected shape (verify at execute time):

    ```latex
    \begin{table}[h]
    \centering
    \footnotesize
    \renewcommand{\arraystretch}{0.92}
    \begin{tabularx}{\textwidth}{@{}lXXXX@{}}
    \toprule
    \textbf{Compared Rate} & \textbf{MAE vs 10\%} & \textbf{Max Abs Error} & \textbf{n\_samples} & \textbf{Caveat} \\
    \midrule
    0.01\% & <FILL>  & <FILL>  & <FILL>   & below 200-sample floor \\
    0.1\%  & <FILL>  & <FILL>  & <FILL>   & --- \\
    1\%    & 0.0378* & <FILL>  & <FILL>   & sanity gate PASS (<0.05) \\
    \bottomrule
    \end{tabularx}
    \caption{SHARDS self-convergence on the 1M-access trace against the 10\%
    reference rate. *MAE of 0.0378 exceeds Waldspurger's 0.001 target on a
    different workload; our trace uses $\alpha=0.8$ Zipf which is
    higher-skew than the paper's baselines.}
    \label{tab:shards}
    \end{table}
    ```

    Executor: open results/shards_large/shards_convergence.csv and replace `<FILL>` with the exact CSV values. Do not fabricate.

    4. Subsection "Error-vs-rate figure + MRC overlay" — insert BOTH figures from results/shards_large/figures/:

    ```latex
    \begin{figure}[h]
    \centering
    \begin{minipage}{0.48\textwidth}
    \includegraphics[width=\textwidth]{../results/shards_large/figures/shards_convergence.pdf}
    \end{minipage}\hfill
    \begin{minipage}{0.48\textwidth}
    \includegraphics[width=\textwidth]{../results/shards_large/figures/shards_mrc_overlay.pdf}
    \end{minipage}
    \caption{(Left) MAE and max absolute error vs.\ SHARDS sampling rate on the
    1M trace. (Right) Four-rate MRC overlay showing convergence at 1\% and
    10\% sampling; 0.01\% falls below the 200-sample floor and is flagged.}
    \label{fig:shards_overlay}
    \end{figure}
    ```

    5. Subsection "Findings" (~100 words). Three named claims: (a) MAE(1\%, 10\%) = 0.0378 PASSES the Phase 4 sanity gate of <0.05; (b) at 0.01\% sampling, the 81-sample count falls below the 200-sample Phase 4 D-01 floor and is flagged with an asterisk; (c) the 50K oracle regime (Phase 4 Plan 04-01 D-03) additionally compared against exact stack distances at smaller scale, confirming the scaling-law extrapolation is valid.
  </action>
  <verify>
    <automated>test -f docs/sections/06-shards.tex && grep -q 'section{SHARDS Validation}' docs/sections/06-shards.tex && grep -q 'shards_mrc_overlay' docs/sections/06-shards.tex && grep -q 'shards_convergence' docs/sections/06-shards.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/06-shards.tex` exit 0
    - `grep -q 'section{SHARDS Validation}' docs/sections/06-shards.tex` exit 0
    - `grep -q 'label{sec:shards}' docs/sections/06-shards.tex` exit 0
    - `grep -q 'shards_mrc_overlay.pdf' docs/sections/06-shards.tex` exit 0 (must-have figure per D-12)
    - `grep -q 'shards_convergence.pdf' docs/sections/06-shards.tex` exit 0 (secondary error-vs-rate figure)
    - `grep -q 'label{fig:shards_overlay}' docs/sections/06-shards.tex` exit 0
    - `grep -q 'label{tab:shards}' docs/sections/06-shards.tex` exit 0
    - `grep -q 'cite{waldspurger2015shards}' docs/sections/06-shards.tex` exit 0
    - `grep -q '0.0378\|0.037\|MAE' docs/sections/06-shards.tex` exit 0 (sanity gate number)
    - `grep -q '200-sample\|200 sample' docs/sections/06-shards.tex` exit 0 (D-01 floor caveat)
    - No FILL placeholder remains: `grep -q '<FILL>' docs/sections/06-shards.tex` returns exit 1 (no literal <FILL>)
    - `wc -w < docs/sections/06-shards.tex` returns value > 250
  </acceptance_criteria>
  <done>§6 exists; contains the 4-rate self-convergence table with actual CSV values (no <FILL>); embeds both shards_convergence.pdf and shards_mrc_overlay.pdf; states the 0.0378 sanity gate, the 200-sample floor caveat, and cites the SHARDS paper.</done>
</task>

<task type="auto">
  <name>Task 4: Write §7 Ablations (all 3 ablation figures in a single dedicated section per D-14)</name>
  <files>docs/sections/07-ablations.tex</files>
  <read_first>
    - .planning/STATE.md Phase 04-03 entry (S3-FIFO ratio ablation findings — smaller small-queue monotonically wins, 6.3pp at Court α=1.2 vs 1.2pp Congress)
    - .planning/STATE.md Phase 04-04 entry (SIEVE visited-bit ablation findings — SIEVE-NoProm monotonically loses, +15.4pp Congress α=1.0 peak, +11.0pp Court α=1.1 peak)
    - .planning/STATE.md Phase 04-05 entry (Doorkeeper ablation findings — context-dependent, -0.72pp Court α=1.1 peak win, +0.52pp Court α=0.6 slight loss, Congress ±0.22pp noise)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-13 (ablation_doorkeeper is 2nd-tier must-have) + D-14 (dedicated Ablations section)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md lines 82-93 (tabular patterns)
  </read_first>
  <action>
    Create docs/sections/07-ablations.tex (~400-500 words, ~50 lines). Per D-14, ALL three ablation figures live in this one section as a rigor signal. Structure:

    1. `\section{Ablations}\label{sec:ablations}` + opening paragraph explaining WHY three ablations (decomposes each policy's design decisions). Frame: "Do the specific design choices in S3-FIFO, SIEVE, and W-TinyLFU matter? We hold the policy fixed and vary one parameter at a time."

    2. Subsection "S3-FIFO small-queue ratio" (~100 words + figure). Motivation: Yang et al. (SOSP'23) recommend 10\% small-queue by default; does this generalize? Method: ran {5\%, 10\%, 20\%} across α ∈ {0.6..1.2} on both workloads. Finding: smaller small-queue ratios monotonically win; 6.3pp gap at Court α=1.2 vs 1.2pp on Congress — a publishable result against the 10\% default. Cite `\cite{yang2023fifo}`.

    ```latex
    \begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{../results/congress/figures/ablation_s3fifo.pdf}
    \caption{S3-FIFO small-queue ratio ablation: 5\%, 10\%, 20\% variants on
    Congress (left) and Court (right) across $\alpha \in \{0.6, \ldots, 1.2\}$
    at cache\_frac=0.01.}
    \label{fig:ablation_s3fifo}
    \end{figure}
    ```

    NOTE: results/congress/figures/ablation_s3fifo.pdf is a 2-panel figure (Congress + Court) per Phase 4 Plan 04-03 STATE.md ("2-panel Congress|Court shared y-axis"). One includegraphics suffices. Use the Congress figure path (alternatively the Court path would work — both are 2-panel, choose congress for consistency with §3's Congress-first convention).

    3. Subsection "SIEVE visited-bit (lazy promotion)" (~100 words + figure). Motivation: Zhang et al. (NSDI'24) claim lazy promotion is the key SIEVE innovation; does it quantitatively matter? Method: toggle promote_on_hit flag, sweep α. Finding: SIEVE-NoProm monotonically loses to SIEVE at every α on both workloads; peak gap +15.4pp Congress α=1.0 and +11.0pp Court α=1.1 — empirically confirms Zhang et al.'s NSDI'24 lazy-promotion claim. Cite `\cite{zhang2024sieve}`.

    ```latex
    \begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{../results/congress/figures/ablation_sieve.pdf}
    \caption{SIEVE visited-bit ablation --- with promotion (solid) vs.\ without
    (dashed) across $\alpha$ at cache\_frac=0.01 on both workloads.}
    \label{fig:ablation_sieve}
    \end{figure}
    ```

    4. Subsection "W-TinyLFU + Doorkeeper" (~150 words + figure). Motivation: TinyLFU paper (Einziger-Friedman) describes a pre-CMS Bloom filter "doorkeeper" to absorb one-hit-wonder traffic; Caffeine's production W-TinyLFU omits it. Does it help in practice? Method: W-TinyLFU with vs without Doorkeeper across α on both workloads. Finding: context-dependent with gap at α extremes — Congress within ±0.22pp (noise); Court shows -0.72pp peak win at α=1.1 and a slight +0.52pp loss at α=0.6. Interpretation: marginal hedge against OHW spikes on heavy-tailed Court trace; consistent with Caffeine's production decision to omit. Cite `\cite{einziger2017tinylfu}` and `\cite{caffeine2024}`.

    ```latex
    \begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{../results/congress/figures/ablation_doorkeeper.pdf}
    \caption{W-TinyLFU $\pm$ Doorkeeper ablation across $\alpha$ at
    cache\_frac=0.01. Shared brown color; solid = baseline W-TinyLFU, dashed =
    +DK variant.}
    \label{fig:ablation_dk}
    \end{figure}
    ```

    5. Subsection "Ablation takeaway" (~75 words). One-paragraph summary: SIEVE's lazy-promotion is load-bearing (biggest ablation delta, +15pp); S3-FIFO benefits from smaller small-queues than the paper default (actionable for practitioners); Doorkeeper is context-dependent (workload-sensitive hedge, not a default). Together these ablations justify our policy ordering is stable across reasonable parameter variations.
  </action>
  <verify>
    <automated>test -f docs/sections/07-ablations.tex && grep -q 'section{Ablations}' docs/sections/07-ablations.tex && grep -c 'includegraphics.*ablation_' docs/sections/07-ablations.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/07-ablations.tex` exit 0
    - `grep -q 'section{Ablations}' docs/sections/07-ablations.tex` exit 0
    - `grep -q 'label{sec:ablations}' docs/sections/07-ablations.tex` exit 0
    - `grep -c 'includegraphics.*ablation_' docs/sections/07-ablations.tex` returns 3 (three ablation figures per D-14)
    - `grep -q 'ablation_doorkeeper' docs/sections/07-ablations.tex` exit 0
    - `grep -q 'ablation_s3fifo' docs/sections/07-ablations.tex` exit 0
    - `grep -q 'ablation_sieve' docs/sections/07-ablations.tex` exit 0
    - `grep -q 'label{fig:ablation_s3fifo}' docs/sections/07-ablations.tex` exit 0
    - `grep -q 'label{fig:ablation_sieve}' docs/sections/07-ablations.tex` exit 0
    - `grep -q 'label{fig:ablation_dk}' docs/sections/07-ablations.tex` exit 0
    - `grep -q '15.4\|15\\%' docs/sections/07-ablations.tex` exit 0 (SIEVE-NoProm peak gap per Phase 04-04)
    - `grep -q '6.3\|6\\.3pp' docs/sections/07-ablations.tex` exit 0 (S3-FIFO Court α=1.2 gap per Phase 04-03)
    - `grep -q '0.72\|0.52\|0.22' docs/sections/07-ablations.tex` exit 0 (Doorkeeper pp numbers per Phase 04-05)
    - `grep -c 'cite{' docs/sections/07-ablations.tex` returns value >= 3 (SIEVE + S3-FIFO + TinyLFU cites)
    - `wc -w < docs/sections/07-ablations.tex` returns value > 350
  </acceptance_criteria>
  <done>§7 exists with all 3 ablation figures (per D-14), each with its own subsection + named finding + quantitative number from Phase 4 STATE.md entries; at least 3 citations; ≥350 words; figure labels set for cross-reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 6 Plan 04 introduces no new trust boundaries — it authors LaTeX source
files that are static content. No new input surface, no network calls, no
parser of untrusted data, no privilege change. All figures referenced are
gitignored (regenerated via `make plots`); the .tex files embed numbers
read from committed planning docs and JSON/CSV artifacts.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | Body .tex contents misrepresenting data | mitigate | Every numeric claim in the plan must be grounded in a listed source artifact (STATE.md phase-decisions, workload_characterization.md, aggregated CSVs); the "Do not invent statistics" directive in Task 2 action + grep acceptance criteria for specific numbers (0.231, 1.028, 15.4, 6.3, 0.72 etc.) are the enforcement surface |
| T-06-04-02 | Information Disclosure | LaTeX \input{} chain exposing unintended file contents | n/a | \input targets are constrained to docs/sections/ which contains only paper-body content; the chain is not user-controlled at build time |
</threat_model>

<verification>
Once Plan 05 lands (so all 9 section files exist), running `make paper`
should produce a valid PDF. Phase-level checks:
- `make paper` exits 0
- `pdfinfo docs/DOC-02-final-report.pdf | grep Pages | awk '{print $2}'` returns > 8
</verification>

<success_criteria>
Plan 04 is complete when:
- docs/sections/{02-related,03-workload,04-policies,06-shards,07-ablations}.tex all exist
- Each file starts with the correct \section{} and ends with a blank line
- All figures cited are those from the interfaces block (no fabricated paths)
- All numeric claims trace back to workload_characterization.md, STATE.md phase entries, or the aggregated CSVs
- All grep-based acceptance criteria in the 4 tasks pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-writeup-demo/06-04-SUMMARY.md`
documenting: the exact numbers pulled from shards_convergence.csv (Task 3
replaces <FILL> placeholders); any deviation from the Phase 4 ablation findings
quoted in the plan (rare — STATE.md should be authoritative); total line and
word counts per section file.
</output>
