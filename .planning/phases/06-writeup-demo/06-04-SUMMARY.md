---
phase: 06-writeup-demo
plan: 04
subsystem: docs
tags: [latex, paper-authoring, tabularx, figure-embedding, workload-characterization, shards-validation, ablations]

# Dependency graph
requires:
  - phase: 06-writeup-demo
    provides: Plan 06-01 LaTeX scaffold (docs/DOC-02-final-report.tex + docs/sections/{01-intro,bibliography}.tex); input{} chain for sections/{02..10}
  - phase: 05-cross-workload-analysis-infrastructure
    provides: 05-ANALYSIS.md prose + results/compare/workload_characterization.md + results/compare/winner_per_regime.md + 5-seed aggregated CSVs with Welch's p-values
  - phase: 04-shards-large-scale-validation-ablations
    provides: results/shards_large/shards_convergence.csv exact MAE/max-err numbers and 3 ablation CSVs + figures (s3fifo, sieve, doorkeeper)
provides:
  - §2 Related Work (docs/sections/02-related.tex) — positioning against SIEVE/S3-FIFO/TinyLFU/Caffeine/SHARDS
  - §3 Workload Characterization (docs/sections/03-workload.tex) — 10-row Congress/Court tabularx + per-workload workload.pdf minipage (W-4) + "raw α differs" mechanism paragraph
  - §4 Policy Comparison (docs/sections/04-policies.tex) — headline "11 of 12" + per-workload alpha_sensitivity figure (D-13) + Welch's p-value citations + robustness narrative
  - §6 SHARDS Validation (docs/sections/06-shards.tex) — 4-rate self-convergence table (verbatim CSV) + shards_mrc_overlay figure + 200-sample floor caveat
  - §7 Ablations (docs/sections/07-ablations.tex) — all 3 ablation figures per D-14 with named findings from Phase 4
affects: [06-05 (Plan 05 authors §5/§8/§9/§10 against these cross-references), 06-06 (pdfinfo gate + `make paper` Makefile target), 06-07 (DOC-02 verification phase)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "LaTeX section-per-file pattern under docs/sections/; main .tex input{}'s each"
    - "tabularx + booktabs + footnotesize + arraystretch=0.92 for all data tables"
    - "Two-panel minipage[0.48\\textwidth] figure idiom for per-workload figure pairs (workload.pdf, alpha_sensitivity.pdf, shards_* pair)"
    - "Numeric-claim sourcing: every number traces to STATE.md phase-decision entry, workload_characterization.md, winner_per_regime.md, or 05-ANALYSIS.md"

key-files:
  created:
    - "docs/sections/02-related.tex"
    - "docs/sections/03-workload.tex"
    - "docs/sections/04-policies.tex"
    - "docs/sections/06-shards.tex"
    - "docs/sections/07-ablations.tex"
  modified: []

key-decisions:
  - "SHARDS table values pulled from Phase 4 Plan 04-01 SUMMARY.md lines 89-92 (authoritative CSV transcription); no regeneration attempted because results/shards_large/ is gitignored per D-15 and the CSV is reproducible in <5s via `make shards-large`"
  - "All numeric claims hedged or explicit: W-TinyLFU vs LRU Congress Welch's p ∈ [1.2e-6, 8.7e-8] cited from STATE.md Plan 05-04 entry (NOT inflated to 10^-8 as a bound); Court W-TinyLFU vs LRU α=1.2 p=0.06 (non-significant) cited to motivate the multi-seed discipline rather than hidden"
  - "Per D-13 revision W-4, §3 now embeds BOTH per-workload workload.pdf figures in a minipage pair (label{fig:workload_viz}) — the workload.pdf was originally 2nd-tier must-have per Phase 6 CONTEXT but W-4 promoted it to a required grounding image"
  - "Per D-14, all 3 ablation figures live together in §7 (not split across §4/§5/§6) — rigor-signal framing with a shared motivation paragraph and a shared takeaway paragraph rather than three disjoint mini-sections"

patterns-established:
  - "Paper-body section template: section{Name}label{sec:name} + 1-sentence opener + subsection*{...} landmarks + figure/table labels for cross-reference"
  - "Figure embedding: includegraphics paths relative to docs/ (../results/.../*.pdf); figures assumed regenerated by `make plots` pre-flight"
  - "Forward/backward cross-reference discipline: §4 forward-refs sec:mechanism (Plan 05); §3 forward-refs sec:bytemrc (Plan 05); §7 back-refs sec:policies (this plan); each label set so downstream sections can cite"

requirements-completed: [DOC-02]

# Metrics
duration: 5min
completed: 2026-04-22
---

# Phase 6 Plan 04: Paper Body Sections Summary

**Five docs/sections/*.tex body files (§2 Related Work, §3 Workload Characterization, §4 Policy Comparison, §6 SHARDS Validation, §7 Ablations) authoring the "breadth" half of DOC-02 with all data tables, figure cross-references, and Welch's-t-grounded numeric claims — closes 4 of 5 DOC-02 SC-1 bullets.**

## Performance

- **Duration:** ~5 min (301 s wall-clock)
- **Started:** 2026-04-22T02:00:43Z
- **Completed:** 2026-04-22T02:05:44Z
- **Tasks:** 4
- **Files created:** 5
- **Files modified:** 0

## Accomplishments

- **§2 Related Work** (206 words, 5 citations): SIEVE/S3-FIFO FIFO-based eviction + TinyLFU/Caffeine admission filtering + SHARDS sampling-MRC, framed around our orthogonal 6-policy head-to-head contribution with Welch's-t CI bands.
- **§3 Workload Characterization** (724 words, 10-row tabularx): Congress vs Court raw statistics verbatim from workload_characterization.md (α 0.231 / 1.028, OHW 0.989 / 1.000, 462,490-byte Court max outlier at 335× median); **both per-workload workload.pdf figures embedded per D-13 W-4 revision** (`label{fig:workload_viz}`); "raw α differs" and "why replay-Zipf on both" mechanism paragraphs from 05-ANALYSIS.md.
- **§4 Policy Comparison** (695 words): headline "W-TinyLFU wins 11 of 12 cells" cross-referenced to `fig:opening` (page-1 compare_mrc_2panel.pdf) with SIEVE exception at Congress cache_frac=0.020 noted; per-workload alpha_sensitivity minipage per D-13 (`label{fig:alpha_sens}`); Welch's methodology + Congress p ∈ [1.2e-6, 8.7e-8] cited; Court α=1.2 p=0.06 non-significant case cited as multi-seed-discipline motivator.
- **§6 SHARDS Validation** (542 words, 4-rate table + 2-figure minipage): `tab:shards` carries the verbatim shards_convergence.csv numbers (0.0437/0.2503/81 at 0.01%; 0.0496/0.1898/691 at 0.1%; **0.0378/0.0671/9751 at 1% — sanity gate PASS**); 200-sample floor caveat explicit; 50K-oracle cross-validation framed as finding #3.
- **§7 Ablations** (666 words): all three ablation figures (`ablation_s3fifo`, `ablation_sieve`, `ablation_doorkeeper`) in one section per D-14 with a shared rigor-signal motivation; named findings (6.3pp S3-FIFO Court α=1.2 gap; +15.4pp SIEVE-NoProm Congress α=1.0; -0.72pp Doorkeeper Court α=1.1 win + 0.52pp α=0.6 loss) all traced to Phase 4 STATE.md entries.

## Task Commits

1. **Task 1: §2 Related Work + §3 Workload Characterization** — `8842e63` (docs)
2. **Task 2: §4 Policy Comparison** — `bb604f9` (docs)
3. **Task 3: §6 SHARDS Validation** — `e6ebf6d` (docs)
4. **Task 4: §7 Ablations** — `58c08a5` (docs)

Plan metadata commit (this SUMMARY) will follow separately.

## Files Created

- `docs/sections/02-related.tex` (25 lines, 206 words) — §2 Related Work, 5 citations
- `docs/sections/03-workload.tex` (118 lines, 724 words) — §3 with 10-row tabularx `tab:workload` + workload.pdf minipage `fig:workload_viz`
- `docs/sections/04-policies.tex` (107 lines, 695 words) — §4 with `fig:alpha_sens` + forward-ref to `sec:mechanism`
- `docs/sections/06-shards.tex` (90 lines, 542 words) — §6 with `tab:shards` + `fig:shards_overlay`
- `docs/sections/07-ablations.tex` (106 lines, 666 words) — §7 with all 3 `fig:ablation_{s3fifo,sieve,dk}` labels

## SHARDS Table Values (Task 3 <FILL> replacement)

The plan specified `<FILL>` placeholders for task 3's self-convergence table. I replaced them verbatim from `.planning/phases/04-shards-large-scale-validation-ablations/04-01-SUMMARY.md` lines 89-92 (the exact CSV schema dump at commit time):

| Compared rate | MAE vs 10% | Max abs error | n_samples_compared | Source of truth |
|---------------|------------|---------------|--------------------|-----------------|
| 0.01%         | 0.0437     | 0.2503        | 81                 | 04-01 SUMMARY L90 |
| 0.1%          | 0.0496     | 0.1898        | 691                | 04-01 SUMMARY L91 |
| 1%            | 0.0378     | 0.0671        | 9,751              | 04-01 SUMMARY L92 |

`n_samples_reference = 101,144` (shared across all three rows, reported in the caption). No fabricated values.

## Decisions Made

- **Figure path convention.** Used `../results/congress/figures/ablation_*.pdf` (not the Court path) for the 3 ablation figures. Phase 4 Plan 04-03/04-04/04-05 SUMMARYs all confirm each ablation figure is a 2-panel (Congress | Court, shared y-axis) plot, so one `\includegraphics` suffices. Chose the `congress/` prefix for consistency with §3's Congress-first convention.
- **Welch's p-value phrasing.** Quoted STATE.md's Plan 05-04 range "p ∈ [1.2e-6, 8.7e-8]" literally as a range rather than collapsing to `p < 10^{-6}` (which would understate the tighter points) or `p < 10^{-8}` (which would overstate the looser points). Kept the whole interval for traceability.
- **Court-side honesty.** Included the Court W-TinyLFU vs LRU α=1.2 p=0.06 non-significant result in §4 as a motivator for multi-seed discipline, rather than hiding it. This matches the 05-ANALYSIS.md guidance.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] LaTeX math-mode escape on line 15 of 06-shards.tex**

- **Found during:** Task 3 post-write self-review
- **Issue:** I initially wrote `\$<\!5$\,s` to mean "less than 5 seconds" — but `\$` is an escaped literal dollar and the following `5$` was interpreted as closing an unopened math region. This would have caused a compile error when §6 is included in `make paper`.
- **Fix:** Rewrote as `${<}5\,$s` (proper math-mode bracketing with `{<}` to suppress the relational spacing).
- **Files modified:** docs/sections/06-shards.tex (line 15)
- **Verification:** No `<FILL>` remains; grep acceptance `200-sample` passes literally (fix also converted `$200$-sample` to `200-sample` in one prose sentence at line 21 to match the plan's literal acceptance pattern `grep -q '200-sample\|200 sample'`).
- **Committed in:** `e6ebf6d` (Task 3 commit; both fixes made before the commit landed)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Caught pre-commit; no downstream effect. Plan executed exactly as specified modulo the one LaTeX-syntax repair.

## Issues Encountered

- **Worktree base divergence.** On agent startup, HEAD was at `44f13ea` (Phase 02 completion commit) instead of the expected `0aaa886` (Phase 06-03 merge commit). Resolved via `git reset --mixed 0aaa886` followed by `git checkout HEAD -- .` to restore tracked files; confirmed `docs/sections/` and `docs/DOC-02-final-report.tex` both present before proceeding.
- **Results directories empty.** `results/compare/`, `results/shards_large/`, `results/{congress,court}/figures/` are gitignored per D-15 and contain no rendered figures or CSVs in this worktree. For Task 3's SHARDS table, I used the verbatim CSV transcription preserved in `04-01-SUMMARY.md` lines 89-92 rather than regenerating the artifact. This is traceable, reproducible via `make shards-large`, and preserves the "do not fabricate" directive from the plan action text.
- **None of the figure paths are verified to resolve at build time** because `make plots WORKLOAD=congress`, `make plots WORKLOAD=court`, and `make shards-large` have not been run in this worktree. This is out of scope for Plan 04 (see Phase 06-06 `make paper` gate) but worth flagging.

## W-4 Compliance (D-13 revision — workload.pdf figures)

Per the plan's explicit revision-W-4 acceptance criteria:

- `grep -q 'congress/figures/workload' docs/sections/03-workload.tex` — PASS (line ~71)
- `grep -q 'court/figures/workload' docs/sections/03-workload.tex` — PASS (line ~74)
- `grep -q 'label{fig:workload_viz}' docs/sections/03-workload.tex` — PASS (line ~80)
- `grep -c 'workload' docs/sections/03-workload.tex` — 10 occurrences (need ≥2)

Both per-workload workload.pdf figures are embedded in a `minipage[0.48\textwidth]` pair with a shared caption and a single `label{fig:workload_viz}` for downstream cross-reference.

## Next Phase Readiness

- Ready for Plan 06-05 parallel execution: disjoint file surface confirmed (06-04 writes to `{02-related, 03-workload, 04-policies, 06-shards, 07-ablations}.tex`; 06-05 writes to `{05-mechanism, 08-byte-mrc, 09-conclusion, 10-appendix}.tex`).
- Cross-reference surface defined: Plan 06-05 should use `sec:mechanism` (§5), `sec:bytemrc` (§8), `sec:conclusion` (§9) labels to match the forward-references in §3 and §4 of this plan.
- Plan 06-06 can gate on `make paper` exit 0 + `pdfinfo | grep Pages | awk '{print $2}'` > 8 once all 9 body sections land.

## Self-Check

Verification commands run post-SUMMARY:

```bash
for f in 02-related 03-workload 04-policies 06-shards 07-ablations; do
  [ -f "docs/sections/${f}.tex" ] && echo "FOUND: ${f}.tex" || echo "MISSING: ${f}.tex"
done
for h in 8842e63 bb604f9 e6ebf6d 58c08a5; do
  git log --oneline --all | grep -q "${h}" && echo "FOUND: ${h}" || echo "MISSING: ${h}"
done
```

## Self-Check: PASSED

All 5 section files present, all 4 task commits present in git log.

---

*Phase: 06-writeup-demo*
*Completed: 2026-04-22*
