---
phase: 06-writeup-demo
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - docs/sections/05-mechanism.tex
  - docs/sections/08-byte-mrc.tex
  - docs/sections/09-conclusion.tex
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "§5 Mechanism is the 1-2-page centerpiece per D-03 — contains the SIEVE-ties-W-TinyLFU-on-Congress mechanism argument"
    - "§5 includes the per-α gap table (Congress vs Court) showing ±0.005 gaps on Congress vs stable +4-5pp on Court"
    - "§8 Byte-MRC carries the σ=0.21 warning about the 462KB Court outlier — both claims and practitioner guidance"
    - "§9 Conclusion opens with winner_per_regime_bar.pdf and closes with the D-05 practitioner decision tree (text + one table/flowchart)"
    - "§9 Limitations subsection acknowledges V2-01..V2-06 deferred items so readers know they are recognized, not invisible (per CONTEXT.md Deferred Ideas)"
  artifacts:
    - path: "docs/sections/05-mechanism.tex"
      provides: "Mechanism deep-dive — 600-900 words, the 1-2-page centerpiece per D-03, with per-α gap table and SIEVE=admission-filter-in-disguise argument"
      min_lines: 70
    - path: "docs/sections/08-byte-mrc.tex"
      provides: "Byte-MRC methodological warning — 300-400 words, the σ=0.21 story for the Court 462KB outlier"
      min_lines: 35
    - path: "docs/sections/09-conclusion.tex"
      provides: "Conclusion with D-05 practitioner decision tree, winner_per_regime_bar figure, and limitations/future-work paragraph listing V2-01..V2-06"
      min_lines: 50
  key_links:
    - from: "docs/sections/05-mechanism.tex"
      to: ".planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md"
      via: "adapted prose from §Why SIEVE Ties W-TinyLFU on Congress"
      pattern: "visited-bit|admission|binary.*gradient"
    - from: "docs/sections/08-byte-mrc.tex"
      to: "05-ANALYSIS.md §Why Court's Byte-Miss-Ratio Has σ=0.21"
      via: "462KB outlier explanation + CI-band recommendation"
      pattern: "462|0.21|outlier"
    - from: "docs/sections/09-conclusion.tex"
      to: "results/compare/figures/winner_per_regime_bar.pdf"
      via: "includegraphics at top of section (D-05 conclusion figure)"
      pattern: "winner_per_regime_bar"
---

<objective>
Author the paper's three closing sections — §5 Mechanism Deep-Dive (the D-03
centerpiece), §8 Byte-MRC Warning (heavy-tailed workload methodology note),
and §9 Conclusion (D-05 practitioner decision tree + limitations pointing
to V2-01..V2-06). These are the "depth" sections per D-03, contrasted with
Plan 04's "breadth" sections.

Purpose: §5 is the direct response to the professor's midpoint "explain why
one algorithm wins" ask. §8 is the methodological warning for other
practitioners working with long-tailed workloads. §9 is the take-home payoff —
the practitioner decision tree the paper builds toward from page 1.

Output: Three docs/sections/*.tex files filling the 3 remaining \input{} slots
in docs/DOC-02-final-report.tex (Plan 01 left 10 stubs; Plan 04 fills 5;
Plan 05 fills 3; Plan 01's intro+bibliography fill the remaining 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-writeup-demo/06-CONTEXT.md
@.planning/phases/06-writeup-demo/06-PATTERNS.md

<!-- Primary prose source for ALL THREE sections in this plan -->
@.planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md

<!-- Data sources for §9 decision tree -->
@results/compare/winner_per_regime.md
@results/compare/workload_characterization.md

<interfaces>
Figure paths (all already rendered):
§9 conclusion opener: ../results/compare/figures/winner_per_regime_bar.pdf

Bibliography keys:
\cite{zhang2024sieve} \cite{yang2023fifo} \cite{waldspurger2015shards}
\cite{einziger2017tinylfu} \cite{caffeine2024} \cite{clauset2009powerlaw}

Per-α gap table source (for §5 centerpiece — copy NUMBERS verbatim from 05-ANALYSIS.md lines 106-120):
Congress W-TinyLFU vs SIEVE gap (negative = SIEVE wins):
  α=0.6: +0.0020    α=0.9: -0.0004
  α=0.7: -0.0002    α=1.0: -0.0047  <- SIEVE wins
  α=0.8: +0.0014    α=1.1: -0.0019  <- SIEVE wins
                    α=1.2: -0.0016  <- SIEVE wins
Gap always within +-0.005 = less than 1σ. These two policies are tied on Congress.

Court W-TinyLFU vs SIEVE gap:
  α=0.6: +0.0418    α=1.0: +0.0469
  α=0.7: +0.0492    α=1.1: +0.0393
  α=0.8: +0.0543    α=1.2: +0.0359
  α=0.9: +0.0540
Stable +4-5pp across every α. W-TinyLFU decisively wins on Court.

Byte-MRC data (for §8, from 05-ANALYSIS.md lines 179-187):
Court cache_frac=0.01, 5-seed mean +/- σ:
| Policy    | Object miss    | Byte miss        |
| W-TinyLFU | 0.728 +- 0.045 | 0.611 +- 0.207   |
| SIEVE     | 0.783 +- 0.045 | 0.656 +- 0.205   |
| LRU       | 0.847 +- 0.026 | 0.755 +- 0.171   |
Object-miss σ: 0.03-0.05. Byte-miss σ: 4-7x larger.

Winner per regime (from results/compare/winner_per_regime.md):
Small Cache (cache_frac=0.001)  Congress=W-TinyLFU(0.869)  Court=W-TinyLFU(0.831)
High Skew (α in {1.0,1.1,1.2}) Congress=SIEVE(0.351)       Court=W-TinyLFU(0.386)
Mixed Sizes (byte-MRC single-seed) Congress=N/A            Court=W-TinyLFU(0.284)
OHW Regime (cache_frac=0.01)   Congress=W-TinyLFU(0.712)   Court=W-TinyLFU(0.728)

Deferred items (from 06-CONTEXT.md §Deferred) — §9 Limitations must mention these:
V2-01 Caffeine trace cross-validation
V2-02 LHD or AdaptSize as 7th policy
V2-03 Third trace domain (SEC EDGAR)
V2-04 Multi-seed α in {1.3, 1.4, 1.5} on Congress
V2-05 Multi-seed byte-MRC aggregation
V2-06 Code review WR-01/WR-02 latent-coupling fixes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write §5 Mechanism Deep-Dive (the D-03 centerpiece)</name>
  <files>docs/sections/05-mechanism.tex</files>
  <read_first>
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md lines 98-170 ("Why SIEVE Ties W-TinyLFU on Congress") — this is the prose source, adapt directly; lines 371-445 (Addendum with refreshed Court data + Congress α ∈ {1.3, 1.4, 1.5} crossover test) — extends the mechanism claim across a wider α grid
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-03 (one deep finding + breadth elsewhere — this is THE deep finding)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md §"specifics" line 158 (analysis-section centerpiece framing)
    - results/compare/aggregated/congress/alpha_sensitivity_aggregated.csv (head + filter for W-TinyLFU vs SIEVE at cache_frac=0.01 — verify per-α gaps cited match)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md lines 82-93 (tabular patterns)
  </read_first>
  <action>
    Create docs/sections/05-mechanism.tex (~600-900 words, ~70 lines). This is the D-03 1-2-page centerpiece. Structure:

    1. `\section{Why SIEVE Ties W-TinyLFU on Congress (and Loses on Court)}\label{sec:mechanism}` + opening paragraph (~75 words): "The most interesting finding this paper produced contradicts the TinyLFU paper's narrative that W-TinyLFU dominates on skewed traces. On Congress, the W-TinyLFU vs SIEVE gap is always within ±0.005 (less than 1σ) across α ∈ {0.6, ..., 1.5} — the two policies are tied. On Court, W-TinyLFU wins by a stable 4-5pp at every α. The explanation is not about the policies. It's about the workloads."

    2. Subsection "Data" (~150 words) — the two per-α gap tables side-by-side. Copy numbers VERBATIM from 05-ANALYSIS.md lines 106-120. Format:

    ```latex
    \begin{table}[h]
    \centering
    \footnotesize
    \renewcommand{\arraystretch}{0.92}
    \begin{tabularx}{\textwidth}{@{}l|XX|XX@{}}
    \toprule
      & \multicolumn{2}{c|}{\textbf{Congress}} & \multicolumn{2}{c}{\textbf{Court}} \\
    \textbf{$\alpha$} & \textbf{W-TLFU} & \textbf{Gap (SIEVE$-$W)} & \textbf{W-TLFU} & \textbf{Gap (SIEVE$-$W)} \\
    \midrule
    0.6 & -- & $+0.0020$ & -- & $+0.0418$ \\
    0.7 & -- & $-0.0002$ & -- & $+0.0492$ \\
    0.8 & -- & $+0.0014$ & -- & $+0.0543$ \\
    0.9 & -- & $-0.0004$ & -- & $+0.0540$ \\
    1.0 & -- & $-0.0047$* & -- & $+0.0469$ \\
    1.1 & -- & $-0.0019$* & -- & $+0.0393$ \\
    1.2 & -- & $-0.0016$* & -- & $+0.0359$ \\
    \bottomrule
    \end{tabularx}
    \caption{W-TinyLFU vs SIEVE miss-ratio gap at cache\_frac=0.01 on both
    workloads. Negative gap (*) means SIEVE wins. Congress: always within
    $\pm 0.005$ = less than $1\sigma$. Court: stable $+4$--$5$pp in favor of
    W-TinyLFU across all $\alpha$.}
    \label{tab:mechanism_gap}
    \end{table}
    ```

    3. Subsection "Hypothesized mechanism: SIEVE's visited-bit as a poor man's admission filter" (~250 words). Adapted from 05-ANALYSIS.md lines 124-148. Key claims:

    - SIEVE's visited-bit eviction is functionally similar to TinyLFU's admission filter, discovered through a different implementation path.
    - On a miss, a new key enters the cache with visited=false. On a hit, the bit flips true. Eviction scans forward, clearing visited=true bits and skipping, evicting the first visited=false key.
    - **Effectively, SIEVE evicts one-hit wonders aggressively.** That is functionally similar to TinyLFU's "admit only if frequency ≥ victim."
    - On Congress (α_raw=0.231, uniform sizes, 91.7% unique keys): the raw trace gives almost no frequency signal. W-TinyLFU's CMS sees essentially-all-ones counters (most keys seen once or twice). The admission filter becomes close to a no-op. Meanwhile SIEVE's visited-bit is equally effective at rejecting OHWs after one cache-cycle. The two policies converge.
    - On Court (α_raw=1.028, long-tail sizes, real locality): the raw trace has real structure. W-TinyLFU's CMS captures a *gradient* of frequencies — key A seen 5 times, B seen 2 times, C once. Admission filter preserves hot keys decisively. SIEVE's *binary* visited-bit loses this gradient — a key seen once and a key seen fifty times are indistinguishable to the scanner. Hence W-TinyLFU pulls ahead by ~5pp.

    Cite `\cite{zhang2024sieve}` and `\cite{einziger2017tinylfu}` in this subsection.

    4. Subsection "Addendum: does the ordering invert at higher α?" (~150 words). Adapted from 05-ANALYSIS.md lines 397-425 (the Phase 5 addendum). We extended the α grid on Congress to {1.3, 1.4, 1.5} with a single-seed run. Result: the crossover hypothesis partially holds — W-TinyLFU edges SIEVE by 0.0008 at α ∈ {1.4, 1.5}, but this is below the 5-seed σ ≈ 0.007-0.010 at these α values. The stronger conclusion: SIEVE and W-TinyLFU are functionally equivalent on Congress across the entire α range from 0.6 to 1.5. Quote the paper claim verbatim from 05-ANALYSIS.md lines 434-442:

    > "On Congress (α_raw=0.23, near-uniform raw trace, uniform object sizes), SIEVE's visited-bit is functionally equivalent to W-TinyLFU's Count-Min Sketch across α ∈ [0.6, 1.5]. On Court (α_raw=1.03, long-tail sizes, real raw locality), W-TinyLFU's frequency gradient captures information SIEVE's binary signal cannot. Choice of admission mechanism matters *only* when the raw workload provides a frequency gradient to exploit."

    5. Subsection "What this means for the TinyLFU narrative" (~100 words). Implication: the TinyLFU paper's "W-TinyLFU wins on Zipf" claim implicitly assumes Zipf is the whole story. Our data shows raw trace structure matters *independently* of the α overlay. This is a more nuanced claim — cite `\cite{caffeine2024}` and note that Caffeine's production decision to omit Doorkeeper is consistent with this finding.

    Reference `\ref{tab:workload}` (Plan 04 §3) for the α_raw=0.231/1.028 anchors.
  </action>
  <verify>
    <automated>test -f docs/sections/05-mechanism.tex && grep -q 'section{Why SIEVE Ties' docs/sections/05-mechanism.tex && grep -q 'label{sec:mechanism}' docs/sections/05-mechanism.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/05-mechanism.tex` exit 0
    - `grep -q 'section{Why SIEVE Ties' docs/sections/05-mechanism.tex` exit 0
    - `grep -q 'label{sec:mechanism}' docs/sections/05-mechanism.tex` exit 0
    - `grep -q 'label{tab:mechanism_gap}' docs/sections/05-mechanism.tex` exit 0
    - `grep -q '0.0020\|0.0047\|0.0543\|0.0418' docs/sections/05-mechanism.tex` exit 0 (per-α gap numbers from 05-ANALYSIS.md)
    - `grep -q 'visited-bit' docs/sections/05-mechanism.tex` exit 0 (mechanism vocabulary)
    - `grep -q -i 'admission filter\|admission.{0,20}filter' docs/sections/05-mechanism.tex` exit 0
    - `grep -q -i 'gradient\|binary' docs/sections/05-mechanism.tex` exit 0 (CMS gradient vs SIEVE binary argument)
    - `grep -q '0.231' docs/sections/05-mechanism.tex` exit 0 (Congress α_raw anchor)
    - `grep -q '1.028\|1.03' docs/sections/05-mechanism.tex` exit 0 (Court α_raw anchor)
    - `grep -q '1.3\|1.4\|1.5' docs/sections/05-mechanism.tex` exit 0 (addendum α extension)
    - `grep -c 'cite{' docs/sections/05-mechanism.tex` returns value >= 2
    - `wc -w < docs/sections/05-mechanism.tex` returns value > 500
  </acceptance_criteria>
  <done>§5 exists as the D-03 centerpiece: has the per-α gap table (both workloads), the SIEVE-as-admission-filter argument, the α ∈ {1.3..1.5} addendum, and ≥500 words. This is the "why" section the professor explicitly asked for.</done>
</task>

<task type="auto">
  <name>Task 2: Write §8 Byte-MRC Methodological Warning</name>
  <files>docs/sections/08-byte-mrc.tex</files>
  <read_first>
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md lines 173-215 ("Why Court's Byte-Miss-Ratio Has σ=0.21 Across Seeds") — prose source, adapt directly
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md lines 244-283 ("Court Has Much Higher Variance Than Congress") — secondary source on seed-to-seed variance
    - results/compare/workload_characterization.md (Court max 462,490 bytes, median 1,381 — gives the 335× ratio)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-12 (byte-MRC outlier is paragraph-length treatment per D-03 "breadth")
  </read_first>
  <action>
    Create docs/sections/08-byte-mrc.tex (~300-400 words, ~35 lines). Structure:

    1. `\section{Heavy-Tailed Workloads Require CI Bands on Byte-MRC}\label{sec:byte-mrc}` + opening sentence connecting to §3's max-size outlier callout: "The 462\,KB Court document flagged in Table~\ref{tab:workload} dominates byte-miss-ratio analysis in ways that require methodological care."

    2. Subsection "The data" (~100 words + mini-table). At cache_frac=0.01 on Court, 5-seed mean ± σ:

    ```latex
    \begin{table}[h]
    \centering
    \footnotesize
    \renewcommand{\arraystretch}{0.92}
    \begin{tabularx}{\textwidth}{@{}lXX@{}}
    \toprule
    \textbf{Policy} & \textbf{Object miss-ratio} & \textbf{Byte miss-ratio} \\
    \midrule
    W-TinyLFU & $0.728 \pm 0.045$ & $\mathbf{0.611 \pm 0.207}$ \\
    SIEVE     & $0.783 \pm 0.045$ & $0.656 \pm 0.205$ \\
    LRU       & $0.847 \pm 0.026$ & $0.755 \pm 0.171$ \\
    \bottomrule
    \end{tabularx}
    \caption{Court at cache\_frac=0.01, 5-seed mean $\pm \sigma$. Object-miss
    $\sigma$: $0.03$--$0.05$. Byte-miss $\sigma$: $4$--$7\times$ larger.}
    \label{tab:byte_mrc_variance}
    \end{table}
    ```

    3. Subsection "The mechanism" (~150 words) — adapted from 05-ANALYSIS.md lines 189-205. Court's 462,490-byte max is 335× the 1,381-byte median. That single outlier represents:
    - 0.78% of the working set by bytes (462KB / 59.6MB)
    - 0.007% of the objects (1 / 15,018)

    At cache_frac=0.01 on Court (working set 59.6MB), the cache is ~600KB. That single outlier is ~77% of the cache. Whether it lives in cache for most of the trace is a near-binary signal on byte-miss-ratio. The replay-Zipf seed controls which key gets the highest popularity rank. If the seed assigns the outlier rank-1 (highest popularity), it stays cached → low byte-miss. If the seed assigns rank-10000+, it never enters cache → high byte-miss. No eviction policy can fix this; it's a popularity-assignment artifact.

    4. Subsection "The paper claim" (~100 words). Quote 05-ANALYSIS.md lines 208-213:

    > On a workload with a catastrophic size outlier, byte-level eviction metrics are dominated by whether the outlier object's Zipf rank is high enough to keep it resident. We report both object miss-ratio (policy-sensitive, low variance) and byte miss-ratio (policy-insensitive at this scale, high variance), and flag the difference as a feature of heavy-tailed workloads.

    Practitioner takeaway: byte-miss benchmarks on traces with long tails can be misleading unless CI bands are reported. Multi-seed CI bands are essential for heavy-tailed real-world workloads, not just a matter of statistical rigor.
  </action>
  <verify>
    <automated>test -f docs/sections/08-byte-mrc.tex && grep -q 'section{' docs/sections/08-byte-mrc.tex && grep -q 'label{sec:byte-mrc}' docs/sections/08-byte-mrc.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/08-byte-mrc.tex` exit 0
    - `grep -q 'section{' docs/sections/08-byte-mrc.tex` exit 0 (section header present)
    - `grep -q 'label{sec:byte-mrc}' docs/sections/08-byte-mrc.tex` exit 0 (label set — §3 forward-references it)
    - `grep -q 'label{tab:byte_mrc_variance}' docs/sections/08-byte-mrc.tex` exit 0
    - `grep -q '462\|462{,}490' docs/sections/08-byte-mrc.tex` exit 0 (outlier size)
    - `grep -q '0.207\|0.205\|0.171' docs/sections/08-byte-mrc.tex` exit 0 (at least one byte-miss σ from 05-ANALYSIS.md)
    - `grep -q '0.21\|0.207\|335' docs/sections/08-byte-mrc.tex` exit 0 (σ=0.21 claim OR 335× ratio)
    - `grep -q -i 'CI.{0,10}band\|confidence.{0,10}interval\|5-seed' docs/sections/08-byte-mrc.tex` exit 0 (practitioner takeaway)
    - `grep -q -i 'heavy-tailed\|long-tail\|outlier' docs/sections/08-byte-mrc.tex` exit 0
    - `grep -q 'ref{tab:workload}' docs/sections/08-byte-mrc.tex` exit 0 (back-reference to §3)
    - `wc -w < docs/sections/08-byte-mrc.tex` returns value > 250
  </acceptance_criteria>
  <done>§8 exists, carries the σ=0.21 data table, explains the 462KB outlier mechanism, quotes the "paper claim" verbatim from 05-ANALYSIS.md, and lands the practitioner takeaway that CI bands are necessary for heavy-tailed workloads.</done>
</task>

<task type="auto">
  <name>Task 3: Write §9 Conclusion with D-05 practitioner decision tree + winner_per_regime figure + limitations (V2-01..V2-06)</name>
  <files>docs/sections/09-conclusion.tex</files>
  <read_first>
    - results/compare/winner_per_regime.md (exact numbers for the 4-regime table in the body of §9)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md D-05 (decision tree in conclusion; winner_per_regime_bar is the take-home figure)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md §"specifics" line 159 (D-05 decision-tree closing wording candidate — use as a starting point)
    - .planning/phases/06-writeup-demo/06-CONTEXT.md §"Deferred Ideas" (V2-01..V2-06 — §9 Limitations must list these)
    - .planning/phases/05-cross-workload-analysis-infrastructure/05-ANALYSIS.md lines 343-366 (paper-outline-implications — §7 conclusion framing)
  </read_first>
  <action>
    Create docs/sections/09-conclusion.tex (~450-600 words, ~50 lines). Structure:

    1. `\section{Conclusion}\label{sec:conclusion}` + opening paragraph (~100 words) restating the contribution and the mechanism finding in plain language: "We compared six cache eviction policies on two real legislative/judicial API workloads with 5-seed Welch's-t-backed confidence intervals. W-TinyLFU wins 11 of 12 miss-ratio cells, but with an important caveat: on Congress's near-uniform raw trace, SIEVE's visited-bit is functionally equivalent to W-TinyLFU's Count-Min Sketch across the entire α range from 0.6 to 1.5. The admission-filter benefit is workload-gated."

    2. Subsection "Winner per regime" with the figure + table. Embed winner_per_regime_bar.pdf:

    ```latex
    \begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{../results/compare/figures/winner_per_regime_bar.pdf}
    \caption{Winner-per-regime summary. Each bar shows the winning policy's
    miss-ratio (lower is better) in that regime on each workload. Base
    policies only; ablation variants excluded per~\S\ref{sec:ablations}.}
    \label{fig:winner_bar}
    \end{figure}
    ```

    Then embed the 4-row regime table (from results/compare/winner_per_regime.md):

    ```latex
    \begin{table}[h]
    \centering
    \footnotesize
    \renewcommand{\arraystretch}{0.92}
    \begin{tabularx}{\textwidth}{@{}lXXXX@{}}
    \toprule
    \textbf{Regime} & \textbf{Congress Winner} & \textbf{Cong Miss} & \textbf{Court Winner} & \textbf{Court Miss} \\
    \midrule
    Small Cache (cache\_frac=0.001)     & W-TinyLFU & 0.869 & W-TinyLFU & 0.831 \\
    High Skew ($\alpha \in \{1.0, 1.1, 1.2\}$) & SIEVE & 0.351 & W-TinyLFU & 0.386 \\
    Mixed Sizes (Court byte-MRC, 1 seed) & N/A     & --    & W-TinyLFU & 0.284 \\
    OHW Regime (cache\_frac=0.01)        & W-TinyLFU & 0.712 & W-TinyLFU & 0.728 \\
    \bottomrule
    \end{tabularx}
    \caption{Winner per regime across both workloads. Single-seed for the
    Mixed Sizes Court byte-miss-ratio row (multi-seed byte-MRC deferred to
    v2). Mixed Sizes Congress is N/A because Congress has near-uniform
    object sizes.}
    \label{tab:winner_regime}
    \end{table}
    ```

    3. Subsection "Practitioner decision tree" (~150 words, D-05 closing content). Use the exact wording candidate from CONTEXT.md §Specifics line 159 as starting point, expand:

    Use this flowchart when picking an eviction policy for a public-records API workload:
    - If your raw trace has $\alpha_{\text{raw}} < 0.5$ and near-uniform size distribution (Congress-like): SIEVE or W-TinyLFU are statistically tied across α ∈ [0.6, 1.5]. SIEVE is the simpler choice — it has no admission filter, no frequency sketch, no Count-Min Sketch; just a visited-bit and a hand pointer.
    - If your raw trace has $\alpha_{\text{raw}} > 0.8$ and long-tail size distribution (Court-like): **W-TinyLFU by ~5pp at every α**. The Count-Min Sketch's frequency gradient extracts information SIEVE's binary visited-bit cannot.
    - If you have a catastrophic size outlier (Court-like): W-TinyLFU's admission filter matters most at small cache sizes (cache fitting only a handful of working-set objects), where a single OHW admission evicts 5--10\% of capacity. Report both object and byte miss-ratios with CI bands.
    - Small-caches amplify everything: if cache fraction < 0.01, admission matters. If cache fraction > 0.1, all reasonable policies converge.

    4. Subsection "Limitations and Future Work" (~150 words). List the 6 deferred items from CONTEXT.md:

    - **Caffeine trace cross-validation (V2-01):** Running our W-TinyLFU on a Caffeine-published workload would provide ±2% agreement validation; deferred due to benchmark-trace access.
    - **LHD or AdaptSize as a 7th policy (V2-02):** Size-aware eviction would behave very differently on Court's long-tail sizes.
    - **Third trace domain (V2-03):** SEC EDGAR would complete the public-records trilogy.
    - **Multi-seed α ∈ {1.3, 1.4, 1.5} on Congress (V2-04):** Single-seed crossover data is suggestive but not CI-backed. Cheap to add (~30s wall-clock).
    - **Multi-seed byte-MRC aggregation (V2-05):** Per-seed CSVs already contain byte_miss_ratio; aggregator one-line change.
    - **Code review WR-01 / WR-02 (V2-06):** Latent-coupling fixes in plot_results.py; only manifest if future work adds ablation variants to the multi-seed sweep.

    Close with 1 sentence: "These items do not change any finding reported here; they extend the rigor envelope for future work."
  </action>
  <verify>
    <automated>test -f docs/sections/09-conclusion.tex && grep -q 'section{Conclusion}' docs/sections/09-conclusion.tex && grep -q 'winner_per_regime_bar' docs/sections/09-conclusion.tex</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sections/09-conclusion.tex` exit 0
    - `grep -q 'section{Conclusion}' docs/sections/09-conclusion.tex` exit 0
    - `grep -q 'label{sec:conclusion}' docs/sections/09-conclusion.tex` exit 0
    - `grep -q 'winner_per_regime_bar.pdf' docs/sections/09-conclusion.tex` exit 0 (D-05 conclusion figure)
    - `grep -q 'label{fig:winner_bar}' docs/sections/09-conclusion.tex` exit 0
    - `grep -q 'label{tab:winner_regime}' docs/sections/09-conclusion.tex` exit 0
    - `grep -q '0.869\|0.831\|0.351\|0.386\|0.284\|0.712\|0.728' docs/sections/09-conclusion.tex` exit 0 (at least one winner_per_regime.md number)
    - `grep -q -i 'decision.{0,5}tree\|flowchart\|picking' docs/sections/09-conclusion.tex` exit 0 (D-05 practitioner decision tree)
    - `grep -q -i 'limitations\|future work' docs/sections/09-conclusion.tex` exit 0 (Limitations subsection)
    - `grep -cE 'V2-0[1-6]\|V2 01\|V2 02\|Caffeine.{0,30}cross\|LHD\|AdaptSize\|SEC EDGAR\|byte-MRC\|WR-0' docs/sections/09-conclusion.tex` returns value >= 3 (at least 3 of the 6 deferred items named)
    - `grep -q 'Mixed Sizes.{0,40}single-seed\|1 seed\|one.seed' docs/sections/09-conclusion.tex` exit 0 (correct hedge on Mixed Sizes per winner_per_regime.md footnote)
    - `wc -w < docs/sections/09-conclusion.tex` returns value > 400
  </acceptance_criteria>
  <done>§9 exists with winner_per_regime_bar figure, 4-row regime table, D-05 practitioner decision tree, and a limitations subsection naming ≥3 of the 6 V2 deferred items. The paper ends on an actionable practitioner takeaway per D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 6 Plan 05 introduces no new trust boundaries — authors LaTeX content.
Same posture as Plan 04.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | §5 mechanism claim misrepresenting the data | mitigate | The per-α gap table in §5 must cite 05-ANALYSIS.md numbers verbatim; grep acceptance asserts specific values (0.0020, 0.0047, 0.0543, 0.0418) are present; the Phase 5 addendum data (α ∈ 1.3-1.5) is explicitly labeled "single-seed" in source and MUST be labeled the same way in §5 |
| T-06-05-02 | Information Disclosure | §9 limitations accidentally revealing undocumented future work | accept | V2-01..V2-06 are already public in CONTEXT.md §Deferred; no privileged information is being disclosed |
</threat_model>

<verification>
Once all 10 section files land (Plan 01 + 04 + 05), Plan 06 runs `make paper`
for end-to-end verification. Plan 05's standalone smoke check:

- All 3 labels are defined and grep-discoverable
- All 3 section files contain their target figure or table by the expected filename/label
</verification>

<success_criteria>
Plan 05 is complete when:
- docs/sections/{05-mechanism,08-byte-mrc,09-conclusion}.tex all exist
- §5 has the per-α gap table from 05-ANALYSIS.md lines 106-120 with verbatim numbers
- §5 contains the SIEVE-as-admission-filter-in-disguise argument with Congress vs Court contrast
- §8 contains the byte-miss σ table and the 462KB outlier mechanism explanation
- §9 contains winner_per_regime_bar figure + 4-row regime table + D-05 decision tree + V2 limitations
- All grep-based acceptance criteria in the 3 tasks pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-writeup-demo/06-05-SUMMARY.md`
documenting: any numerical claim that was hedged because source data was
ambiguous; confirmation that §5's per-α gap table matches 05-ANALYSIS.md lines
106-120 verbatim; confirmation that §9's regime-winner table matches
results/compare/winner_per_regime.md verbatim; per-section word and line counts.
</output>
