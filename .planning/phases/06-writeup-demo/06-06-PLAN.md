---
phase: 06-writeup-demo
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-04, 06-05]
files_modified:
  - docs/DOC-02-final-report.pdf
  - README.md
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "docs/DOC-02-final-report.pdf exists with page count > 8"
    - "`make paper` exits 0 (latexmk completes without errors)"
    - "Task 1 pre-flight verifies ALL ~11 expected figure paths exist BEFORE invoking latexmk (per revision B-2 — results/** is gitignored, so figures may be absent on a fresh checkout)"
    - "Task 1 acceptance verifies the latexmk log has 0 'File ... not found' or 'LaTeX Error ... graphic' warnings (per revision W-7)"
    - "The PDF contains references to all 6 bibliography keys (zhang2024sieve, yang2023fifo, waldspurger2015shards, einziger2017tinylfu, caffeine2024, clauset2009powerlaw)"
    - "README.md updated to mention W-TinyLFU as the 6th policy + live demo quickstart pointing to ./demo.sh"
  artifacts:
    - path: "docs/DOC-02-final-report.pdf"
      provides: "Final submission-ready rendered paper, generated by latexmk"
      min_lines: 0
    - path: "README.md"
      provides: "Updated project README with W-TinyLFU in policy table + Live demo section"
      contains: "./demo.sh"
  key_links:
    - from: "make paper"
      to: "docs/DOC-02-final-report.pdf"
      via: "latexmk -pdf invocation from Plan 01 Makefile target"
      pattern: "latexmk.*DOC-02"
    - from: "README.md"
      to: "demo.sh (via Plan 02)"
      via: "markdown code fence with ./demo.sh invocation"
      pattern: "\\./demo\\.sh"
---

<objective>
Execute the final build of DOC-02: run a figure-existence pre-flight (per
revision B-2 — results/** is gitignored so figures may be absent on a fresh
checkout), then `make paper` end-to-end, verify the rendered PDF has the
expected structure (page count, section labels resolve, figures render,
bibliography resolves, latexmk log has zero missing-figure warnings per
revision W-7), and update README.md with Phase 6 additions per 06-PATTERNS.md
§"README.md (MODIFIED)".

Purpose: This is the "assemble + verify" plan that gates Phase 6 SC-1 ("DOC-02
final report exists as a submission-ready PDF"). Runs AFTER Plans 04 + 05
land all 11 section files (10 body + 1 bibliography, per the B-1/W-3 revision
that added the D-11 appendix); depends on Plan 01 for the Makefile target.

Output: docs/DOC-02-final-report.pdf (rendered), README.md (updated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-writeup-demo/06-CONTEXT.md
@.planning/phases/06-writeup-demo/06-PATTERNS.md

<!-- The build recipe lives in Makefile from Plan 01 -->
@Makefile

<!-- The 11 section files land from Plans 01, 04, 05 -->
<!-- (do not @-include them here; they exist at execute time) -->

<!-- README current state and additions spec -->
@README.md

<interfaces>
Section file inventory (must all exist when Plan 06 runs):
Plan 01: docs/sections/01-intro.tex, docs/sections/bibliography.tex
Plan 04: docs/sections/02-related.tex, 03-workload.tex, 04-policies.tex, 06-shards.tex, 07-ablations.tex
Plan 05: docs/sections/05-mechanism.tex, 08-byte-mrc.tex, 09-conclusion.tex, 10-appendix.tex

Figure inventory — all ~11 must exist on disk BEFORE make paper (B-2 pre-flight):
  results/compare/figures/compare_mrc_2panel.pdf            (Plan 01 §1 + page-1 hook)
  results/compare/figures/winner_per_regime_bar.pdf         (Plan 05 §9)
  results/compare/figures/compare_policy_delta.pdf          (Plan 05 §10 appendix)
  results/compare/figures/compare_mrc_overlay.pdf           (Plan 05 §10 appendix)
  results/shards_large/figures/shards_mrc_overlay.pdf       (Plan 04 §6)
  results/shards_large/figures/shards_convergence.pdf       (Plan 04 §6 + Plan 05 §10 dup)
  results/shards_large/figures/shards_error.pdf             (Plan 05 §10 appendix)
  results/congress/figures/alpha_sensitivity.pdf            (Plan 04 §4)
  results/court/figures/alpha_sensitivity.pdf               (Plan 04 §4)
  results/congress/figures/ablation_doorkeeper.pdf          (Plan 04 §7 — 2-panel incl Court)
  results/congress/figures/ablation_s3fifo.pdf              (Plan 04 §7 — 2-panel incl Court)
  results/congress/figures/ablation_sieve.pdf               (Plan 04 §7 — 2-panel incl Court)
  results/congress/figures/workload.pdf                     (Plan 04 §3 — W-4 addition)
  results/court/figures/workload.pdf                        (Plan 04 §3 — W-4 addition)
  results/congress/figures/mrc.pdf                          (Plan 05 §10 appendix)
  results/court/figures/mrc.pdf                             (Plan 05 §10 appendix)
  results/congress/figures/byte_mrc.pdf                     (Plan 05 §10 appendix)
  results/court/figures/byte_mrc.pdf                        (Plan 05 §10 appendix)
  results/congress/figures/ohw.pdf                          (Plan 05 §10 appendix)
  results/court/figures/ohw.pdf                             (Plan 05 §10 appendix)

Regeneration commands (from B-2 diagnostic):
  make plots WORKLOAD=congress    # regenerates results/congress/figures/*.pdf
  make plots WORKLOAD=court       # regenerates results/court/figures/*.pdf
  python3 scripts/compare_workloads.py   # regenerates results/compare/figures/*
  python3 scripts/plot_results.py --workload compare   # regenerates cross-workload figs
  (SHARDS figures are regenerated via `make shards-large` + `make plots WORKLOAD=shards_large`)

README.md additions (per 06-PATTERNS.md §README.md MODIFIED, lines 325-346):
1. H2 "Policies" table (lines 9-15): add W-TinyLFU as a 6th row
2. New H3 "Live demo (60s, 6-policy sweep)" under "Run" section (lines 25-58)
3. Update "Project Structure" tree (lines 99-117) to include demo.sh, docs/
4. "References" section (lines 119-124): add Einziger TinyLFU + Caffeine entries to match DOC-02 bibliography
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Figure-existence pre-flight (B-2), run `make paper`, verify PDF + latexmk log (W-7)</name>
  <files>docs/DOC-02-final-report.pdf</files>
  <read_first>
    - Makefile (confirm `paper` target exists from Plan 01; confirm `.PHONY` lists it)
    - ls docs/sections/ (confirm all 11 .tex files are present — 10 body + 1 bibliography. If any are missing, HALT with a clear diagnostic listing what is missing)
    - docs/DOC-02-final-report.tex (confirm the 11 \input{} stubs match the files in docs/sections/ including the 10-appendix.tex slot added by Plan 01 per B-1/W-3)
    - .gitignore line 33 (`results/**` is gitignored — confirms why the B-2 figure-existence pre-flight is load-bearing)
  </read_first>
  <action>
    **Pre-flight 1 — section files (10 body + 1 bibliography):**

    ```bash
    for f in 01-intro 02-related 03-workload 04-policies 05-mechanism 06-shards 07-ablations 08-byte-mrc 09-conclusion 10-appendix bibliography; do
      test -f "docs/sections/${f}.tex" || echo "MISSING: docs/sections/${f}.tex"
    done
    ```

    If any file is missing, HALT immediately and fail the task — Plans 01/04/05 must have landed first (including the 10-appendix.tex added by Plan 05 Task 4 per W-3).

    **Pre-flight 2 — figure existence (REVISION B-2):** Every figure path the .tex files \includegraphics must exist on disk BEFORE latexmk runs. Since `results/**` is gitignored (`.gitignore:33`), figures are never committed and WILL be absent on any fresh checkout. Without this check, `make paper` fails non-deterministically with cryptic latexmk errors.

    Run this bash loop verbatim and abort on the first missing figure:
    ```bash
    for fig in \
      results/compare/figures/compare_mrc_2panel.pdf \
      results/compare/figures/winner_per_regime_bar.pdf \
      results/compare/figures/compare_policy_delta.pdf \
      results/compare/figures/compare_mrc_overlay.pdf \
      results/shards_large/figures/shards_mrc_overlay.pdf \
      results/shards_large/figures/shards_convergence.pdf \
      results/shards_large/figures/shards_error.pdf \
      results/congress/figures/alpha_sensitivity.pdf \
      results/court/figures/alpha_sensitivity.pdf \
      results/congress/figures/ablation_doorkeeper.pdf \
      results/court/figures/ablation_doorkeeper.pdf \
      results/congress/figures/ablation_s3fifo.pdf \
      results/court/figures/ablation_s3fifo.pdf \
      results/congress/figures/ablation_sieve.pdf \
      results/court/figures/ablation_sieve.pdf \
      results/congress/figures/workload.pdf \
      results/court/figures/workload.pdf \
      results/congress/figures/mrc.pdf \
      results/court/figures/mrc.pdf \
      results/congress/figures/byte_mrc.pdf \
      results/court/figures/byte_mrc.pdf \
      results/congress/figures/ohw.pdf \
      results/court/figures/ohw.pdf; do
        [ -f "$fig" ] || {
          echo "MISSING: $fig — run: make plots WORKLOAD=congress && make plots WORKLOAD=court && python3 scripts/compare_workloads.py && python3 scripts/plot_results.py --workload compare";
          exit 1;
        };
    done
    ```

    If any figure is missing, HALT and surface the diagnostic. The diagnostic tells the user exactly which regeneration command to run. After regeneration, re-run the pre-flight before proceeding.

    Note: §7 Ablations references `results/congress/figures/ablation_*.pdf` but those are 2-panel figures (Congress+Court on a shared axis per Phase 4 Plan 04-03 STATE.md), and a parallel `results/court/figures/ablation_*.pdf` exists. Pre-flight checks BOTH so that if a future planner or executor picks the Court path instead, the check still catches the missing side.

    **Run the build:**

    ```bash
    make paper
    ```

    Expected behavior: latexmk compiles docs/DOC-02-final-report.tex from within the docs/ directory, resolving all `\input{sections/...}` includes (now 11 including the appendix), all `\includegraphics{../results/.../*.pdf}` paths, all `\cite{}` keys against the bibliography, all `\ref{}` cross-references (including the new `sec:appendix`, `fig:workload_viz`, `fig:app_*` labels from W-3/W-4).

    If `make paper` exits non-zero:
    - Read the docs/DOC-02-final-report.log tail (latexmk writes diagnostics there) to identify the failure
    - Most likely failures: missing `\label{}` (add to the correct section file); missing figure file (pre-flight SHOULD have caught this; if latexmk sees a missing figure, the pre-flight list is incomplete — extend it); missing `\cite{}` key (confirm the key is in docs/sections/bibliography.tex)
    - Fix the specific issue (edit the offending section file — do NOT regenerate figures; pre-flight already verified all figures; if a figure is truly missing, return to pre-flight rather than patching latexmk output)
    - Re-run `make paper`
    - Loop until exit 0 or 3 attempts (if 3 attempts fail, HALT with a diagnostic and defer to user)

    **After `make paper` exits 0, verify the PDF:**

    ```bash
    test -f docs/DOC-02-final-report.pdf
    PAGES=$(pdfinfo docs/DOC-02-final-report.pdf | awk '/^Pages/{print $2}')
    test "$PAGES" -gt 8
    ```

    Verify no undefined references (latexmk typically re-runs until stable, but check):
    ```bash
    grep -c 'LaTeX Warning: Reference' docs/DOC-02-final-report.log
    ```
    Expect 0. If non-zero, the \ref{} / \label{} pairing is broken — fix by cross-checking labels in section files.

    Verify no undefined citations:
    ```bash
    grep -c 'LaTeX Warning: Citation' docs/DOC-02-final-report.log
    ```
    Expect 0.

    **REVISION W-7 — verify no silent missing-figure warnings:**
    ```bash
    grep -cE 'LaTeX Warning: File.*not found|! LaTeX Error.*graphic' docs/DOC-02-final-report.log
    ```
    Expect 0. If non-zero, a figure included via `\includegraphics` could not be resolved — usually indicates a typo in a path or an appendix figure whose path wasn't covered by the pre-flight loop. Extend pre-flight, regenerate figures if necessary, and re-run `make paper`.
  </action>
  <verify>
    <automated>test -f docs/DOC-02-final-report.pdf && pdfinfo docs/DOC-02-final-report.pdf | awk '/^Pages/{exit $2 > 8 ? 0 : 1}'</automated>
  </verify>
  <acceptance_criteria>
    - **REVISION B-2 (pre-flight existence loop):** The pre-flight bash `for fig in ... done` loop completes with no `MISSING:` output before `make paper` is invoked. Verify by re-running the loop after the build: every listed path exits 0 under `test -f`.
    - `test -f docs/DOC-02-final-report.pdf` exit 0
    - `pdfinfo docs/DOC-02-final-report.pdf | awk '/^Pages/{print $2}'` returns integer > 8
    - `grep -c 'LaTeX Warning: Reference' docs/DOC-02-final-report.log || true` returns 0 (all \ref{} resolve)
    - `grep -c 'LaTeX Warning: Citation' docs/DOC-02-final-report.log || true` returns 0 (all \cite{} resolve)
    - **REVISION W-7 (latexmk log missing-figure check):** `grep -cE 'LaTeX Warning: File.*not found|! LaTeX Error.*graphic' docs/DOC-02-final-report.log | grep -q '^0$'` exit 0 (zero silent missing-figure warnings in the log)
    - `pdftotext docs/DOC-02-final-report.pdf - 2>/dev/null | grep -q 'W-TinyLFU\|WTinyLFU'` exit 0 (policy name appears in rendered text)
    - `pdftotext docs/DOC-02-final-report.pdf - 2>/dev/null | grep -q 'SIEVE'` exit 0
    - `pdftotext docs/DOC-02-final-report.pdf - 2>/dev/null | grep -q 'CourtListener\|Court'` exit 0
    - `pdftotext docs/DOC-02-final-report.pdf - 2>/dev/null | grep -q 'Waldspurger\|SHARDS'` exit 0 (bibliography resolved)
    - `pdftotext docs/DOC-02-final-report.pdf - 2>/dev/null | grep -q -i 'supplementary.figures\|appendix'` exit 0 (appendix section rendered per W-3)
  </acceptance_criteria>
  <done>Pre-flight (B-2) confirmed all figure paths exist before make paper ran; `make paper` exits 0; docs/DOC-02-final-report.pdf has >8 pages; no unresolved \ref{}, \cite{}, or missing-figure warnings in the log (W-7); pdftotext spot-checks confirm the rendered text contains the expected content (W-TinyLFU, SIEVE, Court, SHARDS, appendix).</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with W-TinyLFU policy + live-demo quickstart + References additions</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — 124 lines per PATTERNS.md)
    - .planning/phases/06-writeup-demo/06-PATTERNS.md §"README.md (MODIFIED)" (lines 325-346 — spec for the four required additions)
    - demo.sh (confirm it exists and is executable from Plan 02)
  </read_first>
  <action>
    Edit README.md in four places per 06-PATTERNS.md §README.md MODIFIED:

    **Edit 1 — Policies table.** Find the H2 "Policies" table (around README.md lines 9-15). Add W-TinyLFU as a 6th row. Edit with the Edit tool by replacing the existing table block to include:

    ```markdown
    | **W-TinyLFU** | Tiny LFU admission filter + 1% window LRU + 99% SLRU; Count-Min Sketch frequency tracking |
    ```

    inserted as the last row (after SIEVE) in the existing table. Preserve the existing column widths and header row.

    **Edit 2 — Live demo section.** Find the "Run" H2 (around lines 25-58). Insert a new H3 "Live demo (60s, 6-policy sweep)" AFTER the existing "Full analysis with alpha sweep and SHARDS" subsection (or before "CLI options" — place it as a distinct mini-section):

    ```markdown
    ### Live demo (60s, 6-policy sweep)

    ```bash
    ./demo.sh
    ```

    Runs `./cache_sim` on a pre-generated 5K-request slice of the Congress trace with
    all 6 policies (LRU, FIFO, CLOCK, S3-FIFO, SIEVE, W-TinyLFU) at 4 cache sizes,
    prints the miss-ratio table, and renders `results/demo/figures/mrc.pdf`. See
    `demo-rehearsal.log` for timing evidence; completes in under 60 seconds.
    ```

    **Edit 3 — Project Structure tree.** Find the "Project Structure" block (around lines 99-117). Add these entries at the appropriate lexicographic positions in the tree:

    After the existing `├── Makefile` line (or wherever fits the visual tree), add:
    ```
    ├── demo.sh                # Live 60s 6-policy sweep demo (DOC-04)
    ├── docs/                  # Final paper + AI-use report (DOC-02, DOC-03)
    │   ├── DOC-02-final-report.tex
    │   └── DOC-03-ai-use-report.md
    ```

    Match the indentation style used in the existing tree (2-space or 4-space as established — preserve the box-drawing characters `├──` / `│   `).

    **Edit 4 — References section.** Find the H2 "References" (lines 119-124). Add two new entries to the bulleted list to match DOC-02's bibliography:

    ```markdown
    - Einziger, G. et al. "TinyLFU: A Highly Efficient Cache Admission Policy." ACM ToS, 2017.
    - Manes, B. "Caffeine v3.1.8: High-performance caching library for Java." 2024.
    ```

    Place them in the list after the Clauset entry (maintain alphabetical-by-author ordering if that's the existing convention; if not, append to end).

    Do NOT modify: the project title H1, the "Build" section, the CLI options block (Phase 6 does not add CLI flags), the "Output" section (results/ still writes the same CSVs), the existing CourtListener references (Phase 3 already updated README for that, or it's deferred — either way, scope is Phase 6-specific changes).
  </action>
  <verify>
    <automated>grep -q 'W-TinyLFU' README.md && grep -q './demo.sh' README.md && grep -q 'demo\.sh.*Live\|Live.{0,20}demo' README.md && grep -q 'einziger2017tinylfu\|Einziger.*TinyLFU' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'W-TinyLFU' README.md` exit 0 (6th policy row added)
    - `grep -q '\\./demo\\.sh' README.md` exit 0 (live-demo section present)
    - `grep -q -i 'live demo\|60s\|6-policy sweep' README.md` exit 0 (section header)
    - `grep -q 'docs/' README.md` exit 0 (docs/ directory in tree)
    - `grep -q -i 'einziger\|TinyLFU' README.md` exit 0 (bibliography parity with DOC-02)
    - `grep -q -i 'caffeine\|Manes' README.md` exit 0 (bibliography parity)
    - `grep -c '^|' README.md` returns value >= 8 (policy table still intact — 6 policies + header + separator = 8 lines minimum starting with |)
  </acceptance_criteria>
  <done>README.md includes W-TinyLFU in the policy table, has a live-demo quickstart section with `./demo.sh` invocation, mentions docs/ in the project-structure tree, and includes Einziger/TinyLFU + Caffeine citations in References to match DOC-02's bibliography.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 6 Plan 06 runs `latexmk` and writes a PDF. No new trust boundaries —
the same `latexmk` is already trusted (used to build progress.pdf). No
network calls, no parser of untrusted data, no privilege change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Denial of Service | latexmk loop or infinite rebuild | accept | latexmk has a built-in iteration cap; Plan 06 caps at 3 attempts with explicit HALT |
| T-06-06-02 | Tampering | README.md edits overwriting unrelated sections | mitigate | Specific Edit tool uses (grep-anchored find/replace), NOT full-file rewrite; Task 2 action is explicit about which 4 sections to touch |
| T-06-06-03 | Denial of Service | Missing figures on fresh checkout cause cryptic latexmk failures | mitigate | Revision B-2 adds a figure-existence pre-flight bash loop enumerating all ~11 figure paths with clear regeneration diagnostics; revision W-7 adds a post-build grep for silent missing-figure warnings in the latexmk log |
</threat_model>

<verification>
End-to-end phase verification (after this plan plus Plan 07 complete):
- `make paper` exits 0
- `docs/DOC-02-final-report.pdf` page count > 8
- Pre-flight figure-existence loop (B-2) passes before build
- latexmk log has 0 missing-figure warnings (W-7)
- All 3 DOC-{02,03,04} artifacts exist
- README.md reflects Phase 6 state
</verification>

<success_criteria>
Plan 06 is complete when:
- Pre-flight figure-existence loop (B-2) succeeds before make paper runs
- docs/DOC-02-final-report.pdf exists with >8 pages, no unresolved \ref, \cite, or missing-figure warnings (W-7)
- README.md includes W-TinyLFU, live-demo quickstart, docs/ in tree, and expanded References
- All acceptance criteria in the 2 tasks pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-writeup-demo/06-06-SUMMARY.md`
documenting: final PDF page count; any `make paper` iterations required to
reach exit 0 (and what was fixed each round); confirmation that the B-2
pre-flight figure-existence loop ran and passed; confirmation that the W-7
latexmk-log grep returned 0; whether any figures had to be regenerated before
the build could succeed (and which regeneration command was used); confirmation
that all 4 README edits landed.
</output>
