---
phase: 01-enabling-refactors-courtlistener-pilot
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - scripts/plot_results.py
  - Makefile
requirements:
  - REFACTOR-04
autonomous: true
objective: Reorganize results/ into per-workload subdirs (congress/, court/, shards_large/, compare/) per D-04/D-05/D-06. Migrate existing Congress CSVs via git mv into results/congress/, add a --workload flag to scripts/plot_results.py (default congress), update Makefile run/run-sweep targets to pass --output-dir results/congress, and parameterize the plot_workload trace-filename on the workload.
must_haves:
  truths:
    - "results/congress/ exists and contains the previously-top-level CSVs (mrc.csv, alpha_sensitivity.csv, one_hit_wonder.csv, shards_mrc.csv) migrated from results/"
    - "results/court/, results/shards_large/, results/compare/ exist as empty stub directories for future phases"
    - "scripts/plot_results.py accepts --workload (default congress) and writes figures under the workload subdir; --results-dir still overrides when passed explicitly (D-05 back-compat)"
    - "Makefile run and run-sweep targets pass --output-dir results/congress so new runs land in the right subdir by default (D-06)"
    - "plot_workload reads traces/{workload}_trace.csv (was hardcoded congress_trace.csv) so Phase 3's court trace will plot without edits"
    - "python3 scripts/plot_results.py --workload congress --traces-dir traces exits 0 and produces PDFs under results/congress/figures/"
  artifacts:
    - path: "scripts/plot_results.py"
      provides: "--workload flag, workload-parameterized trace file, workload-scoped figures dir"
      contains: "--workload"
    - path: "Makefile"
      provides: "Default-runs target results/congress subdir"
      contains: "results/congress"
    - path: "results/congress/"
      provides: "Migrated top-level CSVs"
      contains: "mrc.csv"
  key_links:
    - from: "scripts/plot_results.py main()"
      to: "results/{workload}/ CSV directory"
      via: "os.path.join (\"results\", args.workload)"
      pattern: "os.path.join\\(\"results\", args.workload\\)"
    - from: "Makefile run target"
      to: "results/congress/"
      via: "--output-dir results/congress"
      pattern: "--output-dir results/congress"
---

<objective>
Reorganize the repo's simulation-output tree into per-workload subdirectories so Phases 3-5 can write into parallel trees without colliding with Congress results. The four subdirs required (D-04):

- results/congress/ — existing Congress CSVs migrated here via git mv.
- results/court/ — Phase 3's court-trace output destination (empty stub for now).
- results/shards_large/ — Phase 4's 1M-scale SHARDS output destination (empty stub).
- results/compare/ — Phase 5's cross-workload output destination (empty stub).

Update scripts/plot_results.py per D-05: add --workload NAME flag with default congress; derive results_dir as results/{workload} unless --results-dir is passed explicitly (back-compat). Parameterize plot_workload's hardcoded traces/congress_trace.csv on the workload argument so Phase 3's court trace plots automatically.

Update Makefile per D-06: run and run-sweep targets pass --output-dir results/congress explicitly so the simulator itself remains workload-agnostic (callers choose). Do NOT teach the simulator about workload identity.

Do NOT edit .gitignore — results/ is already ignored (line 31). Because results/ is gitignored, git mv on these files behaves like a plain mv (they're likely untracked). The migration is a local working-tree reorganization; .gitkeep files in the empty stubs will NOT persist via git without exceptions (we are NOT adding exceptions in this plan).

NOTE ON WAVE: This plan is Wave 4 (the final wave). It depends on 01-03 (Wave 3) because both modify scripts/plot_results.py — 01-03 added the schemas comment block and _has_throughput helper; this plan adds --workload argparse plumbing on top. It can run in parallel with 01-06 (also Wave 4) because 01-06 touches disjoint files (scripts/pilot_court_trace.py, traces/court_pilot.csv, results/court/pilot_report.txt).

Purpose: Phase 3 (court collector and sweep) and Phase 4 (1M SHARDS) can write to their own subdirs without retrofitting path logic; Phase 5's compare_workloads.py can join across subdirs with predictable paths.

Output: Reorganized results/ tree; updated Makefile; updated plot_results.py; no C++ changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-enabling-refactors-courtlistener-pilot/01-CONTEXT.md
@.planning/phases/01-enabling-refactors-courtlistener-pilot/01-PATTERNS.md
@.planning/phases/01-enabling-refactors-courtlistener-pilot/01-03-SUMMARY.md
@scripts/plot_results.py
@Makefile
@.gitignore

<interfaces>
Existing plot_results.py main() at lines 296-319 takes --results-dir default "results" and --traces-dir default "traces". Target: add --workload default "congress", make --results-dir default None, derive the effective results_dir from --workload unless --results-dir is passed.

Existing plot_workload at lines 216-265 hardcodes traces/congress_trace.csv at line 219. Target: add a workload parameter with default "congress", use f-string to build traces/{workload}_trace.csv.

Existing Makefile targets at lines 31-39 use mkdir -p results + ./cache_sim (no --output-dir). Target: mkdir -p results/congress + ./cache_sim --output-dir results/congress for both run and run-sweep.

Existing top-level CSVs in results/ (verified by ls):
- results/mrc.csv
- results/alpha_sensitivity.csv
- results/one_hit_wonder.csv
- results/shards_mrc.csv
- results/figures/ (migrate along with CSVs)
- results/validation/ (leave in place — predates workload split, not REFACTOR-04 scope)

Post-01-03 state: scripts/plot_results.py already has a CSV schemas comment block and a `_has_throughput(df)` helper. Preserve both.

Gitignore reality (.gitignore line 31): results/ is top-level-ignored. The migration is mostly plain mv underneath; git mv is used defensively for any tracked file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate existing results into results/congress/ and create empty workload stubs</name>
  <files>results/congress/, results/court/, results/shards_large/, results/compare/</files>
  <read_first>
    - .planning/phases/01-enabling-refactors-courtlistener-pilot/01-CONTEXT.md (D-04)
    - .planning/phases/01-enabling-refactors-courtlistener-pilot/01-PATTERNS.md (section "results/*.csv into results/congress/")
    - .gitignore line 31 (results/ ignored)
    - Actual contents of results/ via ls results/ to confirm which CSVs exist before migration
  </read_first>
  <behavior>
    - After migration, results/congress/ contains the 4 previously-top-level CSVs (mrc.csv, alpha_sensitivity.csv, one_hit_wonder.csv, shards_mrc.csv) and any existing figures/ directory.
    - The top level results/ no longer contains loose .csv files — only the 4 subdirs and any pre-existing validation/ directory.
    - results/court/, results/shards_large/, results/compare/ exist as empty directories.
    - No tracked file is lost; migration uses git mv for tracked files, mv for untracked.
  </behavior>
  <action>
    Run these shell commands from the repo root in order. The loop tolerates both tracked and untracked files.

    1. Create the 4 workload subdirs:

       mkdir -p results/congress results/court results/shards_large results/compare

    2. For each top-level CSV in results/, move it under results/congress/. Use git mv if tracked, plain mv otherwise:

       for f in mrc.csv alpha_sensitivity.csv one_hit_wonder.csv shards_mrc.csv; do
           if [ -f "results/$f" ]; then
               if git ls-files --error-unmatch "results/$f" > /dev/null 2>&1; then
                   git mv "results/$f" "results/congress/$f"
               else
                   mv "results/$f" "results/congress/$f"
               fi
           fi
       done

    3. If results/figures/ exists at top level, move it under congress too:

       if [ -d results/figures ] && [ ! -d results/congress/figures ]; then
           if git ls-files --error-unmatch results/figures > /dev/null 2>&1; then
               git mv results/figures results/congress/figures
           else
               mv results/figures results/congress/figures
           fi
       fi

    4. Do NOT move results/validation/ — that directory predates workload splitting and is not REFACTOR-04 scope.

    5. Do NOT add .gitkeep files to the empty stubs. Because results/ is gitignored, .gitkeep would not persist via git without a .gitignore exception. Document in the SUMMARY that Phase 3 / 4 / 5 writers will create their own subdir on first write.

    6. Do NOT re-run the simulator — we are reorganizing data, not regenerating it.
  </action>
  <verify>
    <automated>test -d results/congress && test -d results/court && test -d results/shards_large && test -d results/compare && test -f results/congress/mrc.csv && test -f results/congress/alpha_sensitivity.csv && test -f results/congress/one_hit_wonder.csv && test -f results/congress/shards_mrc.csv && [ "$(ls results/*.csv 2>/dev/null | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - test -d results/congress && test -d results/court && test -d results/shards_large && test -d results/compare all pass.
    - All 4 expected CSVs are present under results/congress/.
    - ls results/*.csv returns no matches (top-level CSVs fully migrated).
    - results/validation/ if present before the task is still at results/validation/.
    - results/congress/figures/ exists if results/figures/ existed before the task.
  </acceptance_criteria>
  <done>results/ tree reorganized: congress/ holds all pre-existing CSVs and figures; 3 empty stubs exist for court/shards_large/compare; validation/ untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add --workload flag and workload-parameterized trace filename to scripts/plot_results.py</name>
  <files>scripts/plot_results.py</files>
  <read_first>
    - scripts/plot_results.py (entire file — note Plan 03 Task 3 already added a schemas comment block and _has_throughput helper that must be preserved)
    - .planning/phases/01-enabling-refactors-courtlistener-pilot/01-CONTEXT.md (D-05)
    - .planning/phases/01-enabling-refactors-courtlistener-pilot/01-PATTERNS.md (section "scripts/plot_results.py (MODIFY ...)")
  </read_first>
  <behavior>
    - python3 scripts/plot_results.py with no args defaults to --workload congress, reads from results/congress/, writes figures to results/congress/figures/, and reads the workload trace from traces/congress_trace.csv.
    - python3 scripts/plot_results.py --workload court reads from results/court/, writes to results/court/figures/, reads traces/court_trace.csv. Missing CSVs are skipped by the existing "not found" guards in each plot_* function (no crash on the empty court stub).
    - python3 scripts/plot_results.py --results-dir results/congress --traces-dir traces (explicit --results-dir, no --workload) still works — back-compat with any prior tooling.
    - plot_workload reads traces/{workload}_trace.csv (was hardcoded congress_trace.csv).
  </behavior>
  <action>
    Two edits in scripts/plot_results.py.

    EDIT A: Replace the entire body of main() (currently around lines 296-319) with the following. Preserve the existing Plan-03 schemas comment block and _has_throughput helper.

    def main():
        parser = argparse.ArgumentParser(description="Plot cache simulation results")
        parser.add_argument("--workload", default="congress",
                            help="Workload subdir under results/ (default: congress)")
        parser.add_argument("--results-dir", default=None,
                            help="Directory containing CSV outputs "
                                 "(overrides --workload-derived path)")
        parser.add_argument("--traces-dir", default="traces",
                            help="Directory containing raw trace CSVs")
        args = parser.parse_args()

        # Derive results_dir from --workload unless --results-dir is passed
        # explicitly (D-05 back-compat).
        results_dir = args.results_dir or os.path.join("results", args.workload)

        figures_dir = os.path.join(results_dir, "figures")
        os.makedirs(figures_dir, exist_ok=True)

        print(f"Generating figures for workload '{args.workload}' from {results_dir}...")
        plot_mrc(results_dir, figures_dir)
        plot_byte_mrc(results_dir, figures_dir)
        plot_alpha_sensitivity(results_dir, figures_dir)
        plot_ohw(results_dir, figures_dir)
        plot_shards(results_dir, figures_dir)
        plot_shards_error(results_dir, figures_dir)
        plot_workload(args.traces_dir, figures_dir, args.workload)
        print("Done.")

    EDIT B: Modify plot_workload (currently around lines 216-265). Change the signature to accept a workload parameter with default "congress", and replace the hardcoded trace filename.

    Change the def line from:
        def plot_workload(traces_dir, figures_dir):
    to:
        def plot_workload(traces_dir, figures_dir, workload="congress"):

    Change the trace_path assignment (currently line 219) from:
        trace_path = os.path.join(traces_dir, "congress_trace.csv")
    to:
        trace_path = os.path.join(traces_dir, f"{workload}_trace.csv")

    Keep the rest of plot_workload unchanged (histogram, endpoint breakdown, "Skipping workload plot: {trace_path} not found" guard).

    Do NOT change the other plot_* function signatures. Do NOT remove the schemas comment block or _has_throughput helper added in Plan 03 Task 3.

    Smoke-test both invocation modes:
      python3 scripts/plot_results.py --workload congress --traces-dir traces
      python3 scripts/plot_results.py --results-dir results/congress --traces-dir traces
    Both must exit 0 and produce PDFs under results/congress/figures/.
  </action>
  <verify>
    <automated>grep -q 'args.results_dir or os.path.join' scripts/plot_results.py && grep -q 'default="congress"' scripts/plot_results.py && grep -q 'def plot_workload(traces_dir, figures_dir, workload=' scripts/plot_results.py && grep -q '{workload}_trace.csv' scripts/plot_results.py && grep -q 'def _has_throughput(df):' scripts/plot_results.py && python3 scripts/plot_results.py --workload congress --traces-dir traces > /tmp/plot_wflag.log 2>&1 && ! grep -qi traceback /tmp/plot_wflag.log && python3 scripts/plot_results.py --results-dir results/congress --traces-dir traces > /tmp/plot_explicit.log 2>&1 && ! grep -qi traceback /tmp/plot_explicit.log && test -f results/congress/figures/mrc.pdf</automated>
  </verify>
  <acceptance_criteria>
    - grep 'default="congress"' scripts/plot_results.py matches (the new flag).
    - grep 'args.results_dir or os.path.join' scripts/plot_results.py matches (the derivation rule).
    - grep 'def plot_workload(traces_dir, figures_dir, workload=' scripts/plot_results.py matches (parameterized signature).
    - grep '{workload}_trace.csv' scripts/plot_results.py matches (dynamic filename).
    - grep 'def _has_throughput(df):' scripts/plot_results.py still matches (01-03 helper preserved).
    - Both smoke-test commands exit 0 with no "Traceback" in their log output.
    - results/congress/figures/mrc.pdf exists after the congress run.
    - python3 scripts/plot_results.py --workload court --traces-dir traces also exits 0 (all plots "Skipping ... not found" until Phase 3 lands the court data — script must not crash).
  </acceptance_criteria>
  <done>plot_results.py supports --workload, derives results_dir from it with --results-dir override, and plot_workload reads the workload-specific trace filename. 01-03's schemas comment and _has_throughput helper preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Makefile run + run-sweep targets to pass --output-dir results/congress</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (entire file, 43 lines)
    - .planning/phases/01-enabling-refactors-courtlistener-pilot/01-CONTEXT.md (D-06)
    - .planning/phases/01-enabling-refactors-courtlistener-pilot/01-PATTERNS.md (section "results/*.csv into results/congress/" — Makefile call patterns to update)
  </read_first>
  <behavior>
    - make run still builds cache_sim and runs a default simulation; output lands in results/congress/ instead of results/ top level.
    - make run-sweep does the same plus --alpha-sweep --shards; output lands in results/congress/.
    - make plots still invokes the python plotter (which now defaults to --workload congress, so output equivalence is preserved).
    - No new dependencies, no new tools, no behavioral change to the build itself (lines 1-29 untouched).
  </behavior>
  <action>
    Edit Makefile lines 31-43 (the phony run-targets). Replace lines 31-43 exactly with:

    # Quick run with defaults (synthetic Zipf, alpha=0.8). Writes to congress subdir
    # per D-06; callers override --output-dir to target other workloads.
    run: $(TARGET)
    	mkdir -p results/congress
    	./$(TARGET) --output-dir results/congress

    # Full run: MRC + alpha sweep + SHARDS into results/congress/
    run-sweep: $(TARGET)
    	mkdir -p results/congress
    	./$(TARGET) --alpha-sweep --shards --output-dir results/congress

    # Generate plots from results/congress/ (default --workload=congress)
    plots:
    	python3 scripts/plot_results.py

    Keep the tab-indentation convention (Makefile recipe lines MUST start with a literal TAB, not spaces). Do NOT touch lines 1-29 (build rules).

    After the edit:
    - make clean && make — confirm the build still works (exit 0).
    - make run — confirm it runs to completion with a small synthetic default and writes to results/congress/ (the existing CSVs may be overwritten; that is expected and acceptable).
    - make plots — confirm plot generation works.
  </action>
  <verify>
    <automated>grep -q '^run: \$(TARGET)$' Makefile && grep -q '	mkdir -p results/congress$' Makefile && grep -q '	./\$(TARGET) --output-dir results/congress$' Makefile && grep -q '	./\$(TARGET) --alpha-sweep --shards --output-dir results/congress$' Makefile && awk '/^run:/{getline; getline; if ($0 !~ /^\t/) { print "TAB_ERROR"; exit 1 }} END{print "OK"}' Makefile | grep -q '^OK$' && make clean && make && make run > /tmp/make_run.log 2>&1 && grep -q 'Done. Results written to results/congress/' /tmp/make_run.log</automated>
  </verify>
  <acceptance_criteria>
    - grep '^run: \$(TARGET)$' Makefile matches.
    - grep 'mkdir -p results/congress' Makefile matches (in both run and run-sweep recipe bodies).
    - grep -- '--output-dir results/congress' Makefile matches at least twice (run and run-sweep).
    - All recipe lines (those beginning ./$(TARGET) or mkdir) start with a literal TAB character (Makefile formatting rule).
    - make clean && make exits 0.
    - make run exits 0 and its stdout contains the simulator's final line "Done. Results written to results/congress/".
  </acceptance_criteria>
  <done>Makefile run and run-sweep targets target results/congress explicitly; build still works; a default run lands in the new subdir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Pure reorganization of local output directories and build scripts; no network, no credentials.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | File migration loses tracked data | mitigate | Migration script uses git ls-files --error-unmatch to choose git mv vs plain mv per file; no destructive overwrites; validation/ explicitly preserved. |
| T-01-11 | Denial of Service | Stale figures/ path confuses new plotters | mitigate | figures/ is migrated along with CSVs to results/congress/figures/; plot_results.py main() re-creates the figures dir with os.makedirs(exist_ok=True), so stale paths are harmless. |
| T-01-12 | Information Disclosure | User-scoped paths leaking into committed files | accept | Only path strings touched are repo-relative (results/congress, traces/{workload}_trace.csv); no absolute or user-specific paths introduced. |
</threat_model>

<verification>
After all three tasks:
1. results/congress/, results/court/, results/shards_large/, results/compare/ all exist.
2. results/congress/ contains mrc.csv, alpha_sensitivity.csv, one_hit_wonder.csv, shards_mrc.csv.
3. ls results/*.csv returns nothing (no loose CSVs at the top level).
4. python3 scripts/plot_results.py exits 0, writes PDFs under results/congress/figures/.
5. python3 scripts/plot_results.py --workload court exits 0 without tracebacks (even though no court data exists yet).
6. make clean && make && make run exits 0 and writes output to results/congress/.
7. make plots exits 0.
8. 01-03's schemas comment and _has_throughput helper are still present in plot_results.py.
</verification>

<success_criteria>
1. REFACTOR-04 satisfied: results/ is reorganized into 4 workload subdirs; congress data migrated; stubs exist for court/shards_large/compare (D-04).
2. plot_results.py supports --workload with --results-dir override (D-05) and plot_workload reads traces/{workload}_trace.csv.
3. Makefile default targets write to results/congress/ (D-06); simulator itself is unchanged and stays workload-agnostic.
4. Phase 3 (court trace) and Phase 4 (1M SHARDS) have clear, predictable write paths.
5. No behavioral regression for the Congress plotting path.
6. 01-03 artifacts preserved.
</success_criteria>

<output>
After completion, create .planning/phases/01-enabling-refactors-courtlistener-pilot/01-04-SUMMARY.md describing:
- The exact migration commands executed and which files were tracked vs untracked.
- Confirmation that results/validation/ was preserved at its old path.
- A note that the empty stubs (court/, shards_large/, compare/) do NOT have .gitkeep files because results/ is gitignored and adding a .gitignore exception is out of scope.
- Confirmation that --workload court exits 0 against the empty stub.
- Confirmation that 01-03's schemas comment block and _has_throughput helper are still present in plot_results.py.
</output>
